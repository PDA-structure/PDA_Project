---
phase: 260503-cf7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - QT-260503-cf7
user_setup: []

must_haves:
  truths:
    - "tailscale serve --bg 8000 is registered and persists across reboots"
    - "https://catrins-imac.tail568b7e.ts.net/health returns JSON listing both truss2d and frame2d solvers when uvicorn is running on 127.0.0.1:8000"
    - "https://catrins-imac.tail568b7e.ts.net/ui/frame2d/index.html returns 200 when uvicorn is running"
    - "https://catrins-imac.tail568b7e.ts.net/ui/truss2d/index.html returns 200 when uvicorn is running"
    - "tailscale serve status shows the / -> http://127.0.0.1:8000 mapping"
    - "CLAUDE.md API server section documents the tailnet runbook (start, status, reset) using <mac-name>.<tailnet>.ts.net pattern"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Tailscale runbook appended to existing 'API server' section"
      contains: "tailscale serve"
  key_links:
    - from: "tailscale serve --bg 8000"
      to: "http://127.0.0.1:8000 (uvicorn)"
      via: "persistent background tailnet HTTPS proxy"
      pattern: "https://<mac-name>.<tailnet>.ts.net/ -> 127.0.0.1:8000"
    - from: "CLAUDE.md API server section"
      to: "tailscale serve runbook"
      via: "appended block (start uvicorn / tailnet URL pattern / status / reset)"
      pattern: "tailscale serve --bg 8000"
---

<objective>
Wire Tailscale's tailnet HTTPS proxy on top of the FastAPI single-origin server (delivered by quick task 260503-b57) so the user can access `/health`, `/ui/truss2d/index.html`, and `/ui/frame2d/index.html` over `https://catrins-imac.tail568b7e.ts.net/...` from his Windows work laptop after that laptop joins his tailnet (manual user step, out of scope).

Purpose: Step 1b of the deployment path captured in project memory `project_2d_solver_remote_deployment_priority.md`. Removes the local-only `127.0.0.1:8000` boundary so the user can iterate on the 2D solver UIs from his work machine without exposing anything to the public internet (no `tailscale funnel` — explicitly excluded).

Output:
1. Persistent `tailscale serve` background proxy registered (survives reboots).
2. End-to-end verification through the tailnet URL.
3. Brief Tailscale runbook appended to the "API server" section of CLAUDE.md.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@api_server/app.py

<!--
Pre-verified facts (do NOT re-check, treat as given):
- Tailscale CLI 1.96.5 installed at /usr/local/bin/tailscale
- Mac signed in: deandradepaulo@outlook.com
- Tailnet hostname: catrins-imac.tail568b7e.ts.net
- Tailnet IP: 100.108.110.17
- Port 8000 currently free; no existing `tailscale serve` config in place
- StaticFiles mount at /ui present in api_server/app.py (lines 71-75, commit b7bb211)
- Both UIs use API_URL = '' (origin-relative) per commit e64e496
-->

<interfaces>
<!-- Tailscale serve CLI surface used in this plan (only what the executor needs). -->

# Register persistent background HTTPS proxy on the tailnet:
tailscale serve --bg 8000
# Equivalent to: tailscale serve --bg --https=443 http://127.0.0.1:8000
# Result: GET https://<mac-name>.<tailnet>.ts.net/ -> http://127.0.0.1:8000

# Inspect current proxy config:
tailscale serve status

# Tear down all serve config (the documented "undo"):
tailscale serve reset

# Local hostname/tailnet identity (already verified, do not re-check):
#   catrins-imac.tail568b7e.ts.net  (100.108.110.17)
</interfaces>

<hard_constraints>
- DO NOT modify anything in `api_server/`, `ui/`, `solver_core/`, `tests/`, `visualization/`. Config + 1 doc edit ONLY.
- DO NOT touch CORS config (`allow_origins=["*"]` is fine over Tailscale).
- DO NOT add auth, launchd autostart, or `tailscale funnel` (public internet) — all explicitly out of scope.
- DO NOT kill the user's existing uvicorn process if one is running at execute time. If port 8000 is already bound by uvicorn, USE IT for verification and DO NOT kill it.
- DO NOT create any new doc file. Only edit CLAUDE.md.
- The runbook addition must be terse (5-8 lines), factual, no emojis. Match surrounding tone.
- The tailnet hostname `catrins-imac.tail568b7e.ts.net` is user-specific — document the *pattern* `<mac-name>.<tailnet>.ts.net` and reference the user's actual current value as an example only.
</hard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure tailscale serve --bg 8000 and verify end-to-end through tailnet URL</name>
  <files>(no source files modified — this is a host-level config change registered by tailscaled)</files>
  <action>
Register the persistent background Tailscale HTTPS proxy and verify it works end-to-end.

Step 1 — Confirm pre-state (sanity, not re-verification):
- Run `tailscale serve status` and capture output. Expected: empty / "No serve config" (pre-verified — do not error if it shows something else, just record what's there before mutating).

Step 2 — Decide uvicorn handling:
- Run `lsof -nP -iTCP:8000 -sTCP:LISTEN || echo "PORT_FREE"`.
- If output contains `PORT_FREE`: executor must start uvicorn temporarily for the verification step (and stop it at the end of this task).
- If something is already listening on port 8000: ASSUME it is the user's uvicorn. Use it. DO NOT kill it. Skip the start/stop steps below.

Step 3 — Register the persistent proxy:
- Run `tailscale serve --bg 8000`.
- This is equivalent to `tailscale serve --bg --https=443 http://127.0.0.1:8000`. Persists across reboots (registered with tailscaled).
- Record the exit code; non-zero is a failure that must be surfaced.

Step 4 — Start uvicorn temporarily IF AND ONLY IF port 8000 was free in step 2:
- From repo root (`/Users/catrinevans/Documents/pda_project`), start in background:
  `uvicorn api_server.app:app --host 127.0.0.1 --port 8000 > /tmp/cf7-uvicorn.log 2>&1 &`
- Capture the PID. Wait briefly (e.g. `sleep 2`) for boot.
- Confirm boot: `curl -fsS http://127.0.0.1:8000/health` returns the expected JSON. If not, tail `/tmp/cf7-uvicorn.log` and abort.

Step 5 — Verify through the tailnet URL (these are the load-bearing checks):
- `curl -fsS https://catrins-imac.tail568b7e.ts.net/health`
  Expect: JSON containing both `"truss2d"` and `"frame2d"` in the `solvers` list.
- `curl -fsS -o /dev/null -w "%{http_code}\n" https://catrins-imac.tail568b7e.ts.net/ui/frame2d/index.html`
  Expect: `200`.
- `curl -fsS -o /dev/null -w "%{http_code}\n" https://catrins-imac.tail568b7e.ts.net/ui/truss2d/index.html`
  Expect: `200`.
- `curl -fsS https://catrins-imac.tail568b7e.ts.net/ui/frame2d/script.js | grep -F "API_URL = ''"`
  Expect: at least one matching line (confirms the relative-URL UI from b57 is being served via the Tailscale path).

Step 6 — Confirm proxy state:
- Run `tailscale serve status`. Expect output showing the `/ -> http://127.0.0.1:8000` mapping (the exact format is `https://catrins-imac.tail568b7e.ts.net (tailnet only)` followed by `|-- / proxy http://127.0.0.1:8000` or similar). Save the literal output for the SUMMARY.

Step 7 — Cleanup (only if step 4 started uvicorn):
- Kill the uvicorn PID captured in step 4 (`kill <PID>`; if needed `kill -9 <PID>`).
- Confirm port 8000 is free again: `lsof -nP -iTCP:8000 -sTCP:LISTEN` returns nothing.
- Remove `/tmp/cf7-uvicorn.log` if you created it.

Step 8 — Do NOT teardown the tailscale serve config. The whole point is that it persists. The undo command (`tailscale serve reset`) goes in the doc only.

Failure handling:
- If `tailscale serve --bg 8000` fails: surface the exact stderr; do not retry with funnel or any other variant.
- If any tailnet curl fails: capture status, run `tailscale serve status` and `tailscale status` for diagnosis context, then stop. Do not "fix" by widening scope (no funnel, no auth changes, no app changes).
  </action>
  <verify>
    <automated>tailscale serve status | grep -F "127.0.0.1:8000"</automated>
  </verify>
  <done>
- `tailscale serve status` shows the `/` -> `http://127.0.0.1:8000` mapping.
- All four tailnet curl probes from step 5 succeeded with the expected payload / 200 status (when uvicorn was running, either user-supplied or temporarily started).
- If uvicorn was started by the executor, it has been stopped and port 8000 is free.
- If uvicorn was already running (user-owned), it remains untouched.
- No changes to `api_server/`, `ui/`, `solver_core/`, `tests/`, or `visualization/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Tailscale runbook to CLAUDE.md "API server" section</name>
  <files>CLAUDE.md</files>
  <action>
Append a brief Tailscale runbook to the existing `## API server` section in `CLAUDE.md` (currently lines 112-117). Use the Edit tool. Insert AFTER the existing `/health` bullet (line 117) and BEFORE the next section heading `## UI conventions (both truss2d and frame2d)` (line 119).

Required content (5-8 lines, terse, factual, no emojis, match the surrounding bullet style — note the file uses `-` prefix bullets, no asterisks):

```
- Tailnet access (tailscale serve, persistent across reboots): with uvicorn running on `127.0.0.1:8000`, the API and UIs are reachable from any device on the same tailnet via `https://<mac-name>.<tailnet>.ts.net/...` (current host: `https://catrins-imac.tail568b7e.ts.net/`). Endpoints: `/health`, `/ui/truss2d/index.html`, `/ui/frame2d/index.html`, `POST /solve/{truss2d,frame2d}`.
- Tailscale runbook:
  - Register proxy (one-time, persists): `tailscale serve --bg 8000`
  - Inspect: `tailscale serve status`
  - Tear down: `tailscale serve reset`
  - `tailscale funnel` (public internet exposure) is intentionally NOT used — tailnet-only.
```

Constraints on the edit:
- Use the Edit tool with `old_string` = the exact existing `/health` bullet (line 117) so the insertion point is unambiguous; `new_string` = that same bullet followed by the new lines above.
- Do NOT modify any other line of CLAUDE.md.
- Do NOT add a horizontal rule or extra blank lines beyond what is needed for markdown rendering.
- Keep `<mac-name>.<tailnet>.ts.net` as the documented pattern; the user's specific value (`catrins-imac.tail568b7e.ts.net`) appears once as an example.
- Do NOT add any "see also" link to a separate doc; everything lives in CLAUDE.md.

After the edit, re-read the file and confirm:
- The `## API server` section now contains the new bullets.
- Total line count of CLAUDE.md increased by exactly the number of inserted lines.
- No other content changed (`git diff CLAUDE.md` shows only additions in the API server section).
  </action>
  <verify>
    <automated>grep -F "tailscale serve --bg 8000" CLAUDE.md && grep -F "tailscale serve reset" CLAUDE.md && grep -F "<mac-name>.<tailnet>.ts.net" CLAUDE.md && ! grep -F "tailscale funnel" CLAUDE.md | grep -v "intentionally NOT used"</automated>
  </verify>
  <done>
- CLAUDE.md "API server" section contains the start / status / reset commands and the tailnet URL pattern.
- The user-specific hostname appears as an example only; the documented pattern is `<mac-name>.<tailnet>.ts.net`.
- No `tailscale funnel` recommendation anywhere (the only mention is the explicit "intentionally NOT used" note).
- `git diff CLAUDE.md` shows additions only inside the `## API server` section, no edits elsewhere.
  </done>
</task>

</tasks>

<verification>
Whole-task verification:

1. `tailscale serve status` shows the `/` -> `http://127.0.0.1:8000` mapping (persistent / `--bg`).
2. With uvicorn running on 127.0.0.1:8000, all of these succeed from the Mac:
   - `curl -fsS https://catrins-imac.tail568b7e.ts.net/health` → JSON with `truss2d` + `frame2d`
   - `curl -fsS -o /dev/null -w "%{http_code}" https://catrins-imac.tail568b7e.ts.net/ui/frame2d/index.html` → `200`
   - `curl -fsS -o /dev/null -w "%{http_code}" https://catrins-imac.tail568b7e.ts.net/ui/truss2d/index.html` → `200`
3. `git status` after both tasks shows ONLY `CLAUDE.md` modified (plus the new planning artifacts under `.planning/quick/260503-cf7-.../`). No source-tree changes.
4. `grep -F "tailscale serve --bg 8000" CLAUDE.md` matches; `grep -F "tailscale serve reset" CLAUDE.md` matches.
5. `pytest` not required to run (no source changes), but if executor runs it as a sanity check, expect 61/61 green (unchanged from b57).
</verification>

<success_criteria>
- Persistent tailnet HTTPS proxy `https://catrins-imac.tail568b7e.ts.net/` → `http://127.0.0.1:8000` registered via `tailscale serve --bg 8000` and confirmed in `tailscale serve status`.
- End-to-end verification through the tailnet URL succeeded for `/health`, `/ui/frame2d/index.html`, `/ui/truss2d/index.html` while uvicorn was running.
- CLAUDE.md "API server" section now documents start / status / reset, the `<mac-name>.<tailnet>.ts.net` URL pattern, and the user's current host as an example.
- No changes to source code, tests, or any other doc.
- User's existing uvicorn (if any) was not killed.
- The `tailscale funnel` command does NOT appear anywhere in the plan, the executor's actions, or the doc additions (except the single explicit "intentionally NOT used" note).
</success_criteria>

<output>
After completion, create `.planning/quick/260503-cf7-configure-tailscale-serve-to-expose-fast/260503-cf7-SUMMARY.md`.

The SUMMARY MUST explicitly record:
1. **Persistent state change**: `tailscale serve --bg 8000` is now registered with tailscaled and will survive reboots. This is a stateful host-level change, not just a one-shot command.
2. **Undo command**: `tailscale serve reset` (tears down all serve config; safe and idempotent).
3. **Inspect command**: `tailscale serve status`.
4. **Working tailnet URLs** (for the user's current host): `https://catrins-imac.tail568b7e.ts.net/health`, `/ui/frame2d/index.html`, `/ui/truss2d/index.html`.
5. **What was NOT done** (out of scope): adding the Windows work laptop to the tailnet (manual user step), launchd autostart for uvicorn, app auth, `tailscale funnel`.
6. **Verification log**: literal output of `tailscale serve status` after registration, and the four curl probe results.
7. **Files changed**: only `CLAUDE.md`. No source/test changes.
8. **Next step pointer**: user installs Tailscale on Windows work laptop and signs into the same Microsoft account → tailnet URLs become reachable from that machine.
</output>
