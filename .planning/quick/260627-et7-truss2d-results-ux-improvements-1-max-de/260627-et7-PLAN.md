---
phase: quick-260627-et7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/truss2d/script.js
  - ui/truss2d/index.html
autonomous: false
requirements: [ET7-DMAX, ET7-MULTISELECT, ET7-RESULTSTABLE]
must_haves:
  truths:
    - "After solving, the truss2d results panel shows a δ_max row (max nodal displacement magnitude + node)"
    - "When the deflected-shape toggle is on, the canvas annotates δ=…mm at the max-displaced node"
    - "User can shift-click multiple members and apply one A value to all of them in a single prompt"
    - "Selected members are visually highlighted and the selection clears on Esc or empty-space click"
    - "The member results tables show axial force + Tension/Compression sense; the raw σ (MPa) column is gone from the on-screen tables"
    - "Single-member Member-A editing still works exactly as before"
  artifacts:
    - path: "ui/truss2d/script.js"
      provides: "δ_max computation+annotation, multi-select state+handlers, stress-column removal"
    - path: "ui/truss2d/index.html"
      provides: "Results table headers without σ (MPa) column"
  key_links:
    - from: "drawDeflected()"
      to: "labelManager"
      via: "δ label added at max-magnitude node deflected position"
      pattern: "drawDeflected\\(labelManager"
    - from: "canvas click memberA mode"
      to: "selectedMembers set"
      via: "shift-click toggles, plain click applies A to all selected"
      pattern: "selectedMembers"
---

<objective>
Three related truss2d results/UX improvements, all building on the just-shipped per-member Area A feature (quick 260627-dg0). Pure UI / results-presentation work — no solver, adapter, API, or test changes.

1. **Max-deflection parity with frame2d** — add a δ_max results row + canvas annotation (truss2d currently has neither).
2. **Multi-select members for batch Member-A** — shift-click to build a selection, apply one A value to all.
3. **Restructure member results table** — drop the on-screen σ (MPa) column; make axial force + Tension/Compression the primary member output. DISPLAY-ONLY: keep `meta.member_stresses` and the JSON-export `stress_MPa` field untouched.

Purpose: bring truss2d results UX to frame2d parity and reflect the LOCKED design-boundary decision (truss2d reports DEMAND/forces; full design/capacity stays in the marimo calc platform).
Output: updated `ui/truss2d/script.js` and `ui/truss2d/index.html`.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<scope_contract>
- Touch ONLY `ui/truss2d/{script.js, index.html, style.css}`. style.css only if a highlight color/class is genuinely needed (prefer existing `--canvas-*` / accent tokens).
- ZERO changes to `solver_core/`, `api_server/`, `tests/`, `visualization/`, or `ui/frame2d/`.
- All three changes are achievable from existing data: δ_max comes from existing `UG`; multi-select is pure UI; the table change is display-only. No backend change is needed — if you believe one is, STOP and flag it with justification rather than editing backend.
- The API/UI is served by the persistent launchd service with no-cache `/ui` headers — UI-only changes need only a browser hard-refresh, no restart.
</scope_contract>

<interfaces>
<!-- Extracted from ui/truss2d/script.js + index.html — use directly, no exploration needed. -->

DISPLACEMENT DATA (truss = 2 DOF/node, Ux then Uy, 0-based node index):
  UG[node*2]     → Ux (metres)
  UG[node*2 + 1] → Uy (metres)
  Magnitude:  Math.hypot(UG[i*2], UG[i*2+1])

draw() (script.js:550) builds `const labelManager = new LabelManager({ctx, members, dpr})`
  and calls `if (results && chkDeflected.checked) drawDeflected();` at line 572.
  → To annotate, change that call to `drawDeflected(labelManager)` and add the param.

drawDeflected() (script.js:1125): iterates members, computes per-node pixel offsets
  dx =  ux/UNIT*GRID, dy = -uy/UNIT*GRID (UG already in metres; scale = inputScale).
  Deflected screen position of a node = (n.x + dx, n.y + dy).

frame2d δ canvas-label reference (script.js:2128) — mirror this labelManager.add shape:
  text: 'δ=' + (mag*1000).toFixed(3) + ' mm'
  font: 'bold ' + dfFs + 'px ' + MONO_FONT_FAMILY  (dfFs = Math.round(BASE_LABEL_SIZE * labelScale * getSymbolScale()))
  color: cssVar('--canvas-deflected-label')  // confirm this token exists in truss2d; fall back to cssVar('--canvas-deflected') if not
  bgColor: cssVar('--canvas-label-bg'), priority: 50, textAlign:'center', textBaseline:'middle', type:'diagram'

frame2d δ_max RESULTS-row reference (script.js:3401):
  ['δ_max (vert.)', `${(maxUy*1000).toFixed(4)} mm`, `Node ${maxUyNode}`]  // frame uses a summary table

renderResults() (script.js:1606): truss2d has NO summary table — it has
  #tableTension / #tableCompression / #tableReactions / #tableDisplacements.
  Member rows built at 1621-1641 push {idx,f,m,stress}; stress cell at 1633-1637.
  Displacements built at 1665-1671: `<td>${i+1}</td><td>${ux}</td><td>${uy}</td>`.

Member-A click handler (script.js:294-314, mode === 'memberA'):
  globalA from #inputA; current = members[mi].A_override ?? globalA; prompt; parse;
  val>0 → members[mi].A_override = val (cm²) else null; saveHistory(); results=null; draw().

findMemberAt(x, y, tol=8) (script.js:381) → member index or null.
setMode(m) (script.js:175) resets currentMemberStart and toggles .active buttons.
keydown handler (script.js:352): Ctrl/Cmd-Z = undo, Escape = setMode('view').
saveHistory() (script.js:326) snapshots nodes/members/supports/loads/origin.

exportAnalysis stress field (script.js:1360-1367): builds `stress_MPa` from
  res.meta.member_stresses — LEAVE THIS UNTOUCHED (keep stress in JSON export).

index.html results headers (index.html:271, 279):
  <th>Member</th><th>Nodes</th><th>Force (kN)</th><th>&sigma; (MPa)</th>  (both T and C tables)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: δ_max results row + canvas annotation (frame2d parity)</name>
  <files>ui/truss2d/script.js</files>
  <action>
Mirror frame2d's max-deflection feedback for truss2d (2 DOF/node, magnitude-based since truss has Ux AND Uy).

(a) Compute max nodal displacement MAGNITUDE. In `renderResults()` (script.js:1606), after the Nodal Displacements loop (around line 1671), iterate `nodes` computing `mag = Math.hypot(UG[i*2], UG[i*2+1])`, tracking the max and its node index (1-based for display).

(b) Add a δ_max row to the Nodal Displacements table. Insert a bold summary row as the FIRST row of `#tableDisplacements tbody` (use `dBody.insertBefore(tr, dBody.firstChild)` or build it before the per-node loop): `<td><b>δ_max</b></td><td colspan="2">${(maxMag*1000).toFixed(4)} mm @ Node ${maxNode}</td>`. (No index.html header change needed — it reuses the existing 3-column displacements table.) Guard for the empty/zero case (maxMag < 1e-12 → show '—').

(c) Canvas annotation, gated on the existing `chkDeflected` toggle. truss2d already has `chkDeflected` (index.html:218) and `drawDeflected()` (script.js:1125), and `drawDeflected` is only called when the toggle is checked (line 572) — so annotate inside `drawDeflected`. Change the call site at line 572 to `drawDeflected(labelManager)` and add a `labelManager` parameter to the function. Inside `drawDeflected`, while iterating, track the node with the max `Math.hypot(ux,uy)` (raw UG, pre-scale) and remember its DEFLECTED screen position `(n.x + dx, n.y + dy)`. After the member loop, if maxMag > 1e-12, add a `δ=…mm` label via `labelManager.add({...})` mirroring the frame2d shape in <interfaces> (use the magnitude in mm). Verify the color token: use `cssVar('--canvas-deflected-label')` if it exists in truss2d's CSS, else fall back to `cssVar('--canvas-deflected')`.

(d) Parity note (no fix required): briefly confirm in your SUMMARY whether truss2d's reactions display (`chkReactions`) and deflected-shape rendering match frame2d, and note any OTHER gaps you spot (e.g. truss2d has no M_max/V_max because it is axial-only — expected). Do NOT add frame-only quantities.
  </action>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
  </verify>
  <done>renderResults inserts a bold δ_max row (magnitude + node) atop the displacements table; drawDeflected(labelManager) annotates δ=…mm at the max-displaced node when chkDeflected is on; node --check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-select members to batch-apply Member A</name>
  <files>ui/truss2d/script.js</files>
  <action>
Add the ability to select MULTIPLE members and apply one A value to all, building on the existing prompt-driven `memberA` mode (script.js:294-314). There is NO existing member-selection concept (`currentMemberStart` is node-only) — introduce a minimal one.

(a) State: add a module-level `let selectedMembers = new Set();` (stores member indices) near the other UI state (e.g. by `currentMemberStart` at line 153).

(b) Interaction in the `mode === 'memberA'` click branch:
   - `findMemberAt(px, py)` → `mi`.
   - SHIFT-CLICK on a member (`e.shiftKey && mi !== null`): toggle `mi` in `selectedMembers` (add if absent, delete if present), `draw()`, and RETURN (do not prompt). This builds the selection set with visual feedback.
   - PLAIN CLICK on a member (`mi !== null`, no shift):
       • If `selectedMembers.size > 0`: this is the batch-apply trigger. Ensure `mi` is included (add it). Prompt ONCE for the A value (cm²) — reuse the existing prompt text but phrased for "N selected members", defaulting to the global A. On a valid `val > 0`: `saveHistory()` ONCE, set `m.A_override = val` (cm²) for every index in `selectedMembers`; on blank/0/invalid: set `A_override = null` for all (revert to global). Then `selectedMembers.clear()`, `results = null`, `draw()`.
       • If `selectedMembers.size === 0`: fall through to the EXISTING single-member behaviour unchanged.
   - PLAIN CLICK on empty space (`mi === null`) while `selectedMembers.size > 0`: clear the selection and `draw()` (do not place anything).

(c) Visual highlight: in `drawMembers()` (script.js:608), after drawing each member line, if `selectedMembers.has(idx)` overlay a distinct selection stroke (e.g. a thicker semi-transparent accent outline or a dashed highlight) so selected members are obviously picked. Reuse an existing accent token (e.g. the `#ff9800` used by `highlightNode`, or a `--canvas-*` accent) rather than inventing a palette. Keep it clearly distinct from the tension/compression force colors.

(d) Clearing: extend the `keydown` handler (script.js:352) so `Escape` ALSO clears `selectedMembers` (before/in addition to `setMode('view')`). Also clear `selectedMembers` inside `setMode()` (script.js:175) so leaving Member-A mode drops any pending selection. `draw()` after clearing.

(e) Do NOT break single-member editing, undo, or save/load. `selectedMembers` is transient interaction state — do NOT persist it in saveHistory or the JSON model.
  </action>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
  </verify>
  <done>Shift-click toggles members into a highlighted selection set; a following plain click prompts once and applies (or reverts) A_override across all selected members with a single saveHistory; Esc / empty-space click / mode-change clears the selection; single-member editing path is unchanged; node --check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Remove on-screen σ column — force + Tension/Compression as primary output</name>
  <files>ui/truss2d/script.js, ui/truss2d/index.html</files>
  <action>
LOCKED design-boundary decision: truss2d reports DEMAND (forces); full design (capacity/buckling/utilisation) lives in the marimo calc platform. This is a DISPLAY-ONLY change.

(a) index.html — remove the `<th>&sigma; (MPa)</th>` cell from BOTH member tables: `#tableTension` (line 271) and `#tableCompression` (line 279). Resulting header: `<th>Member</th><th>Nodes</th><th>Force (kN)</th>`. The tables are already split into "Tension Members" / "Compression Members" (index.html:268-282), so the T/C sense is already conveyed by the table the row lands in.

(b) script.js `renderResults()` (1621-1641) — drop the stress `<td>`. Remove the `stressStr` line (1633) and the `+ '<td>' + stressStr + '</td>'` from the row HTML (1637). Keep the existing routing (`r.f > 0 → tBody`, else `cBody`). You may also drop the now-unused `stress` capture at 1625-1626 IF nothing else in renderResults uses it (verify), otherwise leave it.

(c) Make force + T/C explicit. Decision (state in SUMMARY): KEEP the two split T/C tables (they already carry the sense) and show the SIGNED axial force in kN — tension positive per truss convention — so `Force (kN)` reads `+12.345` for tension rows and `-8.200` for compression rows. This keeps sign legibility without a redundant tag column. (If during UAT the user prefers magnitude + an explicit T/C tag column instead, that's a trivial follow-up — note the alternative in SUMMARY.)

(d) DO NOT touch the export path. Leave `meta.member_stresses` and the `exportAnalysis` `stress_MPa` field (script.js:1360-1367) exactly as-is — stress stays in the JSON export for completeness, only the on-screen table column is removed. State this decision explicitly in SUMMARY.
  </action>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
  </verify>
  <done>Both on-screen member tables show Member / Nodes / Force (kN) with no σ column; tension/compression sense is conveyed by the split tables (signed kN force); meta.member_stresses and exportAnalysis stress_MPa are untouched; node --check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All three truss2d results/UX improvements: (1) δ_max results row + on-canvas δ annotation, (2) shift-click multi-select batch Member-A, (3) σ-column removed from the member tables (force + T/C sense retained; JSON export stress untouched).
  </what-built>
  <how-to-verify>
The API/UI is served by the persistent launchd service with no-cache /ui headers — no restart needed. Hard-refresh the truss2d UI (Cmd-Shift-R) at the local URL (e.g. http://127.0.0.1:8000/ui/truss2d/index.html) or the tailnet URL.

1. Build a small truss (e.g. 3-4 nodes, a couple of members), add a pinned + roller support and a downward node load, then Solve.
2. **δ_max:** Confirm the Nodal Displacements table shows a bold δ_max row (magnitude in mm @ Node N) at the top. Tick "Show deflected shape" and confirm a δ=…mm annotation appears on the canvas at the most-displaced node. Untick → annotation disappears.
3. **Multi-select Member A:** Enter "Member A" mode. Shift-click 2+ members — confirm each highlights as selected. Plain-click one more member → a single prompt appears; enter e.g. 250. Confirm ALL selected members get the A=250cm² annotation in one undo step (Cmd-Z reverts all at once). Then: shift-select some, press Esc → selection clears. Confirm a single plain-click on one member (no prior selection) still prompts for just that member (unchanged behaviour).
4. **Results table:** Confirm the Tension Members and Compression Members tables show Member / Nodes / Force (kN) ONLY — no σ (MPa) column. Tension forces show positive, compression negative.
5. **Export still has stress:** Click Export / download the results JSON and confirm `stress_MPa` is still present in the member entries (display-only removal).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `node --check ui/truss2d/script.js` passes after every code task.
- No edits outside `ui/truss2d/{script.js, index.html, style.css}`.
- `git diff --stat` shows only truss2d UI files touched.
- meta.member_stresses / exportAnalysis stress_MPa unchanged (grep confirms `stress_MPa` still present in script.js).
</verification>

<success_criteria>
- δ_max row renders in the displacements table and δ annotation draws on-canvas when deflected toggle is on.
- Shift-click multi-select + single-prompt batch A_override works with one-step undo; Esc/empty-click/mode-change clears selection; single-member path unchanged.
- σ column removed from both on-screen member tables; force shown signed (T positive / C negative); JSON export stress retained.
- Browser UAT approved.
</success_criteria>

<output>
After completion, create `.planning/quick/260627-et7-truss2d-results-ux-improvements-1-max-de/260627-et7-SUMMARY.md` recording: the parity-note findings (Task 1d), the signed-force vs magnitude+tag decision (Task 3c), and the explicit "stress kept in export, removed from table only" decision (Task 3d).
</output>
