---
phase: quick-260627-gp1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/truss2d/script.js
autonomous: false
requirements:
  - GP1-01  # per-member effective A on each member/force row
  - GP1-02  # load_combination provenance string
must_haves:
  truths:
    - "Each tension_members / compression_members row carries its own effective A_cm2 (the A actually used in solve, not just the global)"
    - "An overridden member's exported A_cm2 equals its A_override, not the global #inputA value"
    - "A non-overridden member's exported A_cm2 equals the global #inputA value"
    - "The exported pkg has a top-level load_combination string"
    - "Tension/compression split, sorting, and zero-force dropping are unchanged"
  artifacts:
    - path: "ui/truss2d/script.js"
      provides: "Enriched exportAnalysis() with per-member effective A + load_combination provenance"
      contains: "load_combination"
  key_links:
    - from: "exportAnalysis() member-row builder"
      to: "solve() effective-A rule (line ~533)"
      via: "identical (m.A_override != null ? m.A_override : globalA_cm2) resolution"
      pattern: "A_override != null \\? .* : "
---

<objective>
Enrich the EXISTING truss2d `exportAnalysis()` download (`ui/truss2d/script.js`, ~line 1425-1576) so it carries the two remaining fields the marimo EC3 tension handoff (SEED-005 Phase A) needs. The export is already ~90% complete — it already splits into `tension_members[]` / `compression_members[]`, already has metadata, nodes, members, reactions, displacements. This is a SMALL ADDITIVE enrichment to ONE function. NOT a new export.

Two gaps to close:
- **Gap 1 (GP1-01):** add per-member EFFECTIVE area `A_cm2` to every member/force row, resolved by the SAME rule the solve uses.
- **Gap 2 (GP1-02):** add a top-level `load_combination` provenance string.

Purpose: the marimo tension calc sizes each member against its own gross area Ag and needs to document what the forces represent. The exported A MUST equal the A actually analysed, or the calc would size against the wrong area.

Output: enriched `pkg` written by `exportAnalysis()`; same filename, same overall shape, backward-compatible additions.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Executor: use these directly — the contracts are here, do not re-explore. -->

GROUND TRUTH — the effective-A rule, from solve() in ui/truss2d/script.js (~line 520-534).
The export MUST mirror this EXACTLY so exported A == analysed A:
```javascript
const A_cm2 = parseFloat(document.getElementById('inputA').value);   // global, cm²
// ...
const anyA = members.some(m => m.A_override != null);
const A_for_solve = anyA
  ? members.map(m => (m.A_override != null ? m.A_override : A_cm2) * 1e-4)   // per-member, cm²→m²
  : A_cm2 * 1e-4;                                                            // global scalar, cm²→m²
```
So the per-member EFFECTIVE area in cm² is: `m.A_override != null ? m.A_override : globalA_cm2`.
(`m.A_override` is stored in cm²; see lines 321/341. `globalA_cm2` = `parseFloat(#inputA.value)`.)

Member object shape (the `members[]` array): each `m` has `m.start`, `m.end` (0-based node ids),
and optional `m.A_override` (cm², number or null). Already read in exportAnalysis at line 1436
via `var m = members[idx];` inside the member_forces loop.

Current exportAnalysis() row builder (lines 1435-1455) pushes rows of:
`{ member, nodes:[n1,n2 1-based], length_m, force_kN, force_N, stress_MPa }`
then sorts by |force_N| desc and splits into tension (force_N>0) / compression arrays,
dropping |force_N|<1e-3 rows.

Current pkg literal (lines 1538-1565) has: schema_version '1.0', type, timestamp, canvas_image,
metadata{...}, nodes[], members[], tension_members[], compression_members[], reactions[], displacements[].
metadata already carries the GLOBAL A_cm2 (line 1548) and A_m2 — leave those as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-member effective A + load_combination + sense to exportAnalysis()</name>
  <files>ui/truss2d/script.js</files>
  <action>
Edit ONLY the `exportAnalysis(mode)` function (~line 1425-1576). Make four additive changes:

1. **Read the global A once** at the top of the row-building block (it is already read at line 1429 as `A_cm2` — reuse that variable; do NOT re-declare). This is the `globalA_cm2` fallback.

2. **Per-member effective A on each force row (Gap 1, GP1-01).** Inside the `results.member_forces.forEach` row builder (line ~1435), after `var m = members[idx];`, compute the effective area using the SAME rule as solve():
   ```javascript
   var effA_cm2 = (m && m.A_override != null) ? m.A_override : A_cm2;
   ```
   Add to the pushed row object: `A_cm2: parseFloat(effA_cm2.toFixed(4))` and `A_mm2: parseFloat((effA_cm2 * 100).toFixed(2))` (1 cm² = 100 mm²) for the calc's convenience.
   CRITICAL: this `effA_cm2` MUST be `m.A_override != null ? m.A_override : A_cm2` — identical to solve() line ~533 — so the exported A equals the A actually sent to the solver. Do NOT use the global unconditionally; do NOT recompute differently.

3. **sense field on each force row (optional nice-to-have, low cost — include it).** In the same pushed row, add `sense: f > 0 ? 'T' : 'C'` (tension positive per truss convention). This makes a flattened reader trivial and is consistent with which array the row lands in (force_N>0 → tension).

4. **Per-member effective A on the plain `members[]` array too** (so the array mirrors the force rows). In the `members: members.map(...)` builder (line ~1555), compute the same `effA_cm2` for member `m` and add `A_cm2` + `A_mm2` to the returned object, identical rule.

5. **load_combination provenance (Gap 2, GP1-02).** Near the top of `exportAnalysis` (e.g. just after `mode = mode || 'presentation';`), declare a single named const:
   ```javascript
   var LOAD_COMBINATION = 'as-modelled — ULS factored by user (single case)';
   ```
   Then add `load_combination: LOAD_COMBINATION` as a top-level field in the `pkg` literal (alongside `schema_version`, `type`, `timestamp`). Keep it a single const so backlog 999.2 can later set it dynamically.

6. **schema_version bump (optional, worthwhile — do it).** Change `schema_version: '1.0'` to `schema_version: '1.1'` to mark the additive A + load_combination + sense fields. The additions are backward-compatible (existing readers ignore unknown keys), so this is safe.

Do NOT change: the tension/compression split logic, the |force|<1e-3 drop, the sort, the metadata global A_cm2/A_m2 (leave as the model's global), reactions, displacements, canvas image capture, filename, or the checkbox save/restore dance. Touch ONLY `exportAnalysis`. No index.html change. No other function.
  </action>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
  </verify>
  <done>
`node --check ui/truss2d/script.js` passes. exportAnalysis now: (a) every tension/compression row has A_cm2, A_mm2, sense; (b) each members[] entry has A_cm2, A_mm2; (c) per-member A uses `m.A_override != null ? m.A_override : A_cm2` (matches solve line ~533); (d) pkg has top-level load_combination = 'as-modelled — ULS factored by user (single case)' from a single const; (e) schema_version is '1.1'. No other code paths touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Enriched truss2d Export Analysis JSON: per-member effective area on every member/force row (matching the area used in the solve), a top-level `load_combination` provenance string, a `sense` T/C tag per row, and schema_version bumped to 1.1. Tension/compression split preserved.
  </what-built>
  <how-to-verify>
1. Open the truss2d UI (e.g. https://catrins-imac.tail568b7e.ts.net/ui/truss2d/index.html or http://127.0.0.1:8000/ui/truss2d/index.html). Hard-refresh to clear any cached script.js (Cmd-Shift-R).
2. Build a small truss (e.g. 3-4 nodes, 3-5 members) with supports and at least one load.
3. Use "Member A" mode to set a per-member A override on ONE member (e.g. set member 2 to a value clearly different from the global A in the A input box — say global 100 cm², override 250 cm²).
4. Solve. Confirm it solves cleanly.
5. Click Export Analysis (presentation or as-displayed). Open the downloaded `truss2d-analysis-<ts>.json`.
6. Confirm:
   (a) The OVERRIDDEN member's row (in tension_members or compression_members, and in members[]) shows `A_cm2` = its override value (e.g. 250), NOT the global (100). A non-overridden member shows `A_cm2` = the global (100). `A_mm2` = A_cm2 × 100.
   (b) Top-level `load_combination` = "as-modelled — ULS factored by user (single case)" is present.
   (c) `tension_members[]` / `compression_members[]` split is intact, each row has a `sense` of "T" / "C" matching its array, and zero-force members are still dropped.
   (d) `schema_version` is "1.1".
  </how-to-verify>
  <resume-signal>Type "approved" or describe any mismatch (especially if an overridden member's A_cm2 shows the global value instead of its override).</resume-signal>
</task>

</tasks>

<verification>
- `node --check ui/truss2d/script.js` passes (syntax).
- Manual UAT confirms exported per-member A equals the A used in the solve (override resolution mirrors solve line ~533), load_combination present, T/C split intact.
- Scope contract: only `ui/truss2d/script.js` changed; zero solver_core / api_server / tests / other-UI / index.html touches.
</verification>

<success_criteria>
- Every `tension_members` / `compression_members` row and every `members[]` entry carries `A_cm2` (= `m.A_override != null ? m.A_override : globalA_cm2`) and `A_mm2`.
- Overridden member exports its override A; non-overridden member exports the global A.
- Top-level `load_combination` string present from a single named const.
- `sense` "T"/"C" on each force row; `schema_version` "1.1".
- Backward-compatible (additive only); tension/compression split, sort, zero-force drop unchanged.
- Browser UAT approved.
</success_criteria>

<output>
After completion, create `.planning/quick/260627-gp1-truss2d-export-analysis-download-save-js/260627-gp1-SUMMARY.md`.

Note for the record (NOT a task): after this lands, the orchestrator will reconcile the SEED-005 scoping-note "frozen contract" to match this REAL exported shape (split arrays + per-member A_cm2/A_mm2 + load_combination + sense) so the marimo reader (Phase A Step 2) binds to reality.
</output>
