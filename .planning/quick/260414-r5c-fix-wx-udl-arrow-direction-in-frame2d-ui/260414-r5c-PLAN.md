---
phase: quick-260414-r5c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ui/frame2d/script.js]
autonomous: false
requirements: [QUICK-260414-r5c]
must_haves:
  truths:
    - "Positive wx UDL renders arrows pointing in global +X (rightward)"
    - "Negative wx UDL renders arrows pointing in global -X (leftward)"
    - "Arrow baseline (tails) sits opposite to the force direction, tips touch the member"
    - "wx magnitude label stays adjacent to the baseline (behind arrows)"
    - "Vertical wy UDL rendering is unchanged"
    - "Solver behavior and API unchanged"
  artifacts:
    - path: "ui/frame2d/script.js"
      provides: "drawUDLs() with corrected horizontal UDL arrow geometry"
      contains: "baseOffsetX = -arrowLen * sign"
  key_links:
    - from: "ui/frame2d/script.js drawUDLs() (wx loop)"
      to: "solver wx sign convention (positive wx = +X force)"
      via: "arrow geometry: tail behind member opposite to force, tip on member"
      pattern: "baseOffsetX = -arrowLen \\* sign"
---

<objective>
Fix the horizontal UDL (wx) arrow rendering in the frame2d UI so arrow direction matches the solver's sign convention.

Purpose: Currently positive wx produces leftward-pointing arrows while the solver applies a rightward force, confusing users. Renderer-only fix; solver is correct.
Output: Corrected `drawUDLs()` wx loop in `ui/frame2d/script.js`.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ui/frame2d/script.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert wx arrow geometry in drawUDLs()</name>
  <files>ui/frame2d/script.js</files>
  <action>
In `ui/frame2d/script.js`, locate the second loop in `drawUDLs()` (around line 757) that renders horizontal UDL (wx). Make the following renderer-only changes so positive wx draws rightward arrows and negative wx draws leftward arrows:

1. Change baseline offset (around line 773):
   - FROM: `const baseOffsetX = arrowLen * sign;`
   - TO:   `const baseOffsetX = -arrowLen * sign;`
   (The `ctx.moveTo(n1.x + baseOffsetX, n1.y)` / `ctx.lineTo(n2.x + baseOffsetX, n2.y)` lines stay the same — they now use the corrected offset.)

2. Change arrow tail x-coordinate (around line 788):
   - FROM: `const tailX = ax + arrowLen * sign;`
   - TO:   `const tailX = ax - arrowLen * sign;`

3. Change arrowhead base corners (around lines 795–797):
   - FROM:
     ```
     ctx.moveTo(ax, ay);
     ctx.lineTo(ax + 8 * sign, ay - 4);
     ctx.lineTo(ax + 8 * sign, ay + 4);
     ```
   - TO:
     ```
     ctx.moveTo(ax, ay);
     ctx.lineTo(ax - 8 * sign, ay - 4);
     ctx.lineTo(ax - 8 * sign, ay + 4);
     ```

4. Change label position (around line 802):
   - FROM: `ctx.fillText(..., mx + arrowLen * sign * 1.8, my);`
   - TO:   `ctx.fillText(..., mx - arrowLen * sign * 1.8, my);`

Do NOT modify the vertical (wy) UDL loop. Do NOT modify any solver, adapter, API, or model file. This is a pure renderer fix — the solver's wx convention (positive = +X) is already correct.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('ui/frame2d/script.js','utf8');const checks=[/const baseOffsetX = -arrowLen \* sign;/,/const tailX = ax - arrowLen \* sign;/,/ctx\.lineTo\(ax - 8 \* sign, ay - 4\)/,/ctx\.lineTo\(ax - 8 \* sign, ay \+ 4\)/,/mx - arrowLen \* sign \* 1\.8/];const bad=[/const baseOffsetX = arrowLen \* sign;/,/const tailX = ax \+ arrowLen \* sign;/,/ctx\.lineTo\(ax \+ 8 \* sign, ay - 4\)/,/ctx\.lineTo\(ax \+ 8 \* sign, ay \+ 4\)/];for(const r of checks){if(!r.test(s)){console.error('MISSING:',r);process.exit(1);}}for(const r of bad){if(r.test(s)){console.error('STILL PRESENT:',r);process.exit(1);}}console.log('OK');"</automated>
  </verify>
  <done>All five changes applied in the wx loop; no old patterns remain; wy loop untouched; no non-UI files modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Corrected wx UDL arrow direction in frame2d UI.</what-built>
  <how-to-verify>
1. Start the API: `uvicorn api_server.app:app --reload` from `pda_project/`.
2. Open `ui/frame2d/index.html` in a browser.
3. Create a simple horizontal member (e.g. two nodes along +X with a fixed support at the left).
4. Apply a positive wx UDL (e.g. wx = +10 kN/m) to the member.
   - Expected: arrows point RIGHT (global +X), baseline sits to the LEFT of the member, label to the LEFT.
5. Apply a negative wx UDL (wx = -10 kN/m) to another member.
   - Expected: arrows point LEFT (global -X), baseline sits to the RIGHT, label to the RIGHT.
6. Confirm vertical wy UDL rendering is unchanged (positive wy still renders as before — downward arrows with baseline above member).
7. Solve the structure and confirm reactions/results are unchanged vs before the fix (solver was already correct).
  </how-to-verify>
  <resume-signal>Type "approved" if arrows match solver convention, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- Automated pattern check confirms the five code changes are applied and none of the old patterns remain.
- Human verification confirms visual correctness for positive/negative wx and unchanged wy rendering.
</verification>

<success_criteria>
- Positive wx → arrows render rightward; negative wx → arrows render leftward.
- Baseline and label sit opposite to the force direction (behind the arrows).
- Vertical UDL rendering unchanged.
- No changes outside `ui/frame2d/script.js`.
- Solver output identical to before the fix.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-r5c-fix-wx-udl-arrow-direction-in-frame2d-ui/260414-r5c-SUMMARY.md`.
</output>
