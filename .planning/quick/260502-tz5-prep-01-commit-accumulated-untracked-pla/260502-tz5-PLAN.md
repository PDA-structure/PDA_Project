---
phase: 260502-tz5-prep-01-commit-accumulated-untracked-pla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - .planning/debug/convert-missing-section-false.md
  - .planning/notes/2026-04-29-browser-first-3d-vr-strategy.md
  - .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md
  - .planning/seeds/SEED-002-webxr-quest3-walkthrough.md
  - .planning/seeds/SEED-003-construction-extension-via-quantity-takeoff.md
  - .planning/todos/pending/2026-04-29-threejs-frame2d-3d-viewer.md
  - api_server/run_server.py
  - visualization/__init__.py
  - visualization/truss2d_plots.py
autonomous: true
requirements:
  - PREP-01-PLANNING-COMMIT
  - PREP-01-RUN-SERVER-COMMIT
  - PREP-01-VISUALIZATION-COMMIT

must_haves:
  truths:
    - "Three atomic commits land on main, one per semantic chunk (planning artifacts, run_server, visualization package)"
    - "No code/logic changes — content of all files is byte-identical to current worktree"
    - ".planning/config.json modification (_auto_chain_active true → false) is captured in commit 1"
    - "After all three commits, working tree is clean except .claude/ (which remains untracked by design)"
    - "solver_core has no new imports of visualization (leaf rule preserved — verified by grep)"
  artifacts:
    - path: ".planning/debug/convert-missing-section-false.md"
      provides: "Phase-7 ConvertToAnalytical bug record"
    - path: ".planning/notes/2026-04-29-browser-first-3d-vr-strategy.md"
      provides: "Strategy direction note"
    - path: ".planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md"
      provides: "Phase 7 patterns doc"
    - path: ".planning/seeds/SEED-002-webxr-quest3-walkthrough.md"
      provides: "WebXR Quest 3 seed"
    - path: ".planning/seeds/SEED-003-construction-extension-via-quantity-takeoff.md"
      provides: "Quantity takeoff seed"
    - path: ".planning/todos/pending/2026-04-29-threejs-frame2d-3d-viewer.md"
      provides: "Three.js viewer todo"
    - path: "api_server/run_server.py"
      provides: "uvicorn dev launcher (three-line entry point)"
    - path: "visualization/__init__.py"
      provides: "Empty package init for visualization leaf module"
    - path: "visualization/truss2d_plots.py"
      provides: "Truss2D matplotlib plotting (leaf — never imported by solver_core)"
  key_links:
    - from: "git HEAD"
      to: "three new commits"
      via: "git log --oneline -3"
      pattern: "(docs\\(planning\\)|chore\\(api\\)|feat\\(visualization\\))"
    - from: "solver_core/"
      to: "visualization/"
      via: "import statements (must be ABSENT)"
      pattern: "from visualization|import visualization"
---

<objective>
Janitorial commit pass: land three atomic commits for accumulated untracked files in the worktree. No code edits, no logic changes — pure `git add` + `git commit` operations on existing files.

Purpose: Clear `git status` noise before next phase of work. Each commit is a single semantic chunk so the history stays bisectable and reviewable.

Output: Three commits on main:
1. Planning artifacts (config.json modification + 6 new docs/seeds/todos)
2. `api_server/run_server.py` (dev launcher)
3. `visualization/` package (truss2d_plots.py + __init__.py — the leaf plotting module called out in CLAUDE.md)

After completion, working tree is clean except `.claude/` (GSD harness — separate decision pending, intentionally left untracked).
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- Hard rule from CLAUDE.md to remember during the visualization commit:
     "Never import from `visualization/` inside `solver_core/` or `api_server/`."
     This is a NEW package being committed; we must verify the leaf invariant
     still holds after the commit (no solver_core file imports it). -->

<!-- Current git status snapshot (from session start):
     Modified:
       .planning/config.json
     Untracked:
       .claude/                                                              ← EXCLUDE
       .planning/debug/convert-missing-section-false.md
       .planning/notes/2026-04-29-browser-first-3d-vr-strategy.md
       .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md
       .planning/seeds/SEED-002-webxr-quest3-walkthrough.md
       .planning/seeds/SEED-003-construction-extension-via-quantity-takeoff.md
       .planning/todos/pending/2026-04-29-threejs-frame2d-3d-viewer.md
       api_server/run_server.py
       visualization/                                                        ← package (__init__.py + truss2d_plots.py) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit planning artifacts (config + 6 new docs)</name>
  <files>
    .planning/config.json,
    .planning/debug/convert-missing-section-false.md,
    .planning/notes/2026-04-29-browser-first-3d-vr-strategy.md,
    .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md,
    .planning/seeds/SEED-002-webxr-quest3-walkthrough.md,
    .planning/seeds/SEED-003-construction-extension-via-quantity-takeoff.md,
    .planning/todos/pending/2026-04-29-threejs-frame2d-3d-viewer.md
  </files>
  <action>
Stage and commit the seven `.planning/` files in a single atomic commit.

Steps:
1. Run `git status --short` to confirm the seven files are present (1 modified `config.json` + 6 untracked).
2. Run `git diff --stat .planning/config.json` to confirm the change is the documented `_auto_chain_active: true → false` toggle (one-line edit). If the diff shows anything beyond that toggle, STOP and surface it to the user before committing.
3. Stage explicitly by listing each path (do NOT use `git add .` or `git add -A`):
   ```
   git add \
     .planning/config.json \
     .planning/debug/convert-missing-section-false.md \
     .planning/notes/2026-04-29-browser-first-3d-vr-strategy.md \
     .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md \
     .planning/seeds/SEED-002-webxr-quest3-walkthrough.md \
     .planning/seeds/SEED-003-construction-extension-via-quantity-takeoff.md \
     .planning/todos/pending/2026-04-29-threejs-frame2d-3d-viewer.md
   ```
4. Run `git diff --cached --stat` and verify exactly seven paths are staged, all under `.planning/`. No `api_server/`, no `visualization/`, no `.claude/`.
5. Commit with message (HEREDOC for formatting fidelity):
   ```
   git commit -m "$(cat <<'EOF'
   docs(planning): commit accumulated planning artifacts (debug, notes, seeds, phase-07 patterns, todo)

   Janitorial commit — no logic changes. Bundles seven planning artifacts
   accumulated since the last PREP pass:

   - config.json: _auto_chain_active toggled to false
   - debug/convert-missing-section-false.md: resolved Phase-7 ConvertToAnalytical bug
   - notes/2026-04-29-browser-first-3d-vr-strategy.md: strategy direction
   - phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md: phase 7 patterns
   - seeds/SEED-002-webxr-quest3-walkthrough.md
   - seeds/SEED-003-construction-extension-via-quantity-takeoff.md
   - todos/pending/2026-04-29-threejs-frame2d-3d-viewer.md

   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   EOF
   )"
   ```

Do NOT use `--amend`. Do NOT skip hooks. Do NOT touch `.claude/` or any file outside the seven listed paths.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^docs(planning): commit accumulated planning artifacts" && git log -1 --name-only --pretty=format: | grep -E "^\.planning/" | wc -l | tr -d ' ' | grep -q "^7$" && git status --porcelain | grep -v "^?? \.claude/" | grep -E "\.planning/" | wc -l | tr -d ' ' | grep -q "^0$"</automated>
  </verify>
  <done>HEAD commit subject is `docs(planning): commit accumulated planning artifacts (debug, notes, seeds, phase-07 patterns, todo)`; commit touches exactly 7 files all under `.planning/`; `git status` no longer shows any `.planning/` modifications or untracked entries.</done>
</task>

<task type="auto">
  <name>Task 2: Commit api_server/run_server.py uvicorn dev launcher</name>
  <files>api_server/run_server.py</files>
  <action>
Stage and commit the new `api_server/run_server.py` file.

Steps:
1. Read `api_server/run_server.py` and confirm it is the documented three-line uvicorn launcher (`uvicorn.run("app:app", host="127.0.0.1", port=8000, reload=True)` plus the necessary import + `if __name__ == "__main__"` guard if present). If the file contains anything else (route definitions, middleware, business logic), STOP and surface it before committing — the brief specifies a tiny launcher only.
2. Stage the single file:
   ```
   git add api_server/run_server.py
   ```
3. Confirm only that one file is staged: `git diff --cached --name-only` must output exactly `api_server/run_server.py`.
4. Commit (HEREDOC):
   ```
   git commit -m "$(cat <<'EOF'
   chore(api): add run_server.py uvicorn dev launcher

   Three-line dev convenience launcher for the FastAPI app. No behaviour
   change to the API itself — `uvicorn api_server.app:app --reload` from
   the project root remains the canonical command per CLAUDE.md.

   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   EOF
   )"
   ```

Do NOT modify the file's contents. Do NOT add a `__main__.py`. Do NOT touch `api_server/app.py`.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^chore(api): add run_server.py uvicorn dev launcher" && git log -1 --name-only --pretty=format: | grep -E "^api_server/run_server\.py$" && [ "$(git log -1 --name-only --pretty=format: | grep -v '^$' | wc -l | tr -d ' ')" = "1" ]</automated>
  </verify>
  <done>HEAD commit subject is `chore(api): add run_server.py uvicorn dev launcher`; commit touches exactly one file (`api_server/run_server.py`); `git status` no longer shows the file as untracked.</done>
</task>

<task type="auto">
  <name>Task 3: Commit visualization/ package (leaf plotting module)</name>
  <files>visualization/__init__.py, visualization/truss2d_plots.py</files>
  <action>
Stage and commit the new `visualization/` package as a single atomic commit. This package is the leaf plotting module described in CLAUDE.md ("Visualization is always a leaf — it may import from solver_core but solver_core never imports it").

Steps:
1. Verify package shape:
   - `ls visualization/` should list exactly two files: `__init__.py` and `truss2d_plots.py`.
   - `wc -c visualization/__init__.py` should report 0 (empty file) or near-zero. If non-empty, read it and confirm it has no logic — empty package init is the documented shape.
   - `wc -c visualization/truss2d_plots.py` should report ~9386 bytes. Read the head of the file to confirm it uses lazy `import matplotlib` per CLAUDE.md's hard rule (matplotlib import inside each function, not at module top — though a top-level `import matplotlib` is acceptable here because `visualization/` is the leaf, not solver_core).
2. **Leaf invariant check** — run a grep across `solver_core/` and `api_server/` to confirm NEITHER imports anything from `visualization`:
   ```
   grep -rn -E "(from visualization|import visualization)" solver_core/ api_server/
   ```
   This MUST return no matches (exit code 1 / empty output). If it returns matches, STOP — the leaf rule is violated and we cannot commit until it is reconciled.
3. Stage both files explicitly:
   ```
   git add visualization/__init__.py visualization/truss2d_plots.py
   ```
4. Confirm `git diff --cached --name-only` lists exactly those two paths.
5. Commit (HEREDOC):
   ```
   git commit -m "$(cat <<'EOF'
   feat(visualization): add visualization package with truss2d plotting leaf module

   Adds the visualization/ package as the matplotlib plotting leaf, per the
   architecture rule in CLAUDE.md: visualization may import from solver_core,
   but solver_core never imports from visualization. truss2d_plots.py renders
   truss diagnostics from a solved Truss2D / AnalysisResult pair.

   No imports added to solver_core or api_server — leaf invariant verified
   via grep before commit.

   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   EOF
   )"
   ```
6. Final verification: `git status --porcelain` should now show only `?? .claude/` and nothing else.

Do NOT modify either file's contents. Do NOT add visualization to `solver_core` dependencies.
  </action>
  <verify>
    <automated>git log -1 --pretty=%s | grep -q "^feat(visualization): add visualization package" && git log -1 --name-only --pretty=format: | grep -qE "^visualization/__init__\.py$" && git log -1 --name-only --pretty=format: | grep -qE "^visualization/truss2d_plots\.py$" && [ -z "$(grep -rn -E '(from visualization|import visualization)' solver_core/ api_server/ 2>/dev/null)" ] && [ "$(git status --porcelain | grep -v '^?? \.claude/' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>HEAD commit subject is `feat(visualization): add visualization package with truss2d plotting leaf module`; commit touches exactly the two files in `visualization/`; grep confirms no `solver_core/` or `api_server/` file imports `visualization`; `git status --porcelain` shows only `?? .claude/`.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the following end-state checks:

1. `git log --oneline -3` shows three new commits in this order (newest first):
   - `feat(visualization): add visualization package with truss2d plotting leaf module`
   - `chore(api): add run_server.py uvicorn dev launcher`
   - `docs(planning): commit accumulated planning artifacts (debug, notes, seeds, phase-07 patterns, todo)`

2. `git status --porcelain` output is exactly one line: `?? .claude/`

3. Leaf invariant holds: `grep -rn -E "(from visualization|import visualization)" solver_core/ api_server/` returns no matches.

4. Existing test suite still green (smoke check, since no logic changed): `pytest tests/ -x` passes (10 tests).
</verification>

<success_criteria>
- Three atomic commits on main, one per semantic chunk, with the exact subjects listed in `<verification>`.
- Each commit's file list matches its scope exactly (no cross-contamination between planning/api/visualization).
- `.claude/` remains untracked.
- `solver_core/` and `api_server/` contain zero imports of `visualization` (leaf rule preserved).
- `pytest tests/ -x` still passes (no logic changed, but worth confirming).
- No file content was edited — `git show <commit>` for each commit shows pure additions (or, for `config.json`, the single documented one-line toggle).
</success_criteria>

<output>
After completion, create `.planning/quick/260502-tz5-prep-01-commit-accumulated-untracked-pla/260502-tz5-SUMMARY.md` with:
- The three commit SHAs and subjects
- Confirmation that `.claude/` remains the only untracked entry
- Confirmation that `pytest tests/ -x` is green
- Note that this closes the PREP-01 janitorial pass for 2026-05-02
</output>
