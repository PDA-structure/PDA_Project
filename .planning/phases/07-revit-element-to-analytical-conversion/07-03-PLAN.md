---
phase: 07-revit-element-to-analytical-conversion
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/script.py
  - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/icon.png
  - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_minimal_frame.rvt
  - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_multi_storey.rvt
  - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_pre_converted.rvt
  - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/UAT_RUNBOOK.md
autonomous: false
requirements:
  - REVIT-CONVERT-04
must_haves:
  truths:
    - "TaskDialog summary always shown (D-09) with distinct line for already-associated (D-03): 'converted | already-associated | skipped (errors) | total'"
    - "pyRevit Output Window prints a markdown table with output.linkify(element_id) clickable links for every skip (D-08) -- engineer clicks ID and Revit highlights it"
    - "Three RVT fixtures committed under sibling repo fixtures/phase07/: minimal_frame, multi_storey, pre_converted (D-12)"
    - "Phase 5/v1.2 Tier 1 ExportToPDA round-trip on fixture 1 produces canonical PDA JSON that loads in frame2d UI and solves to analytical reference within tolerance (D-13; ROADMAP success criterion 4)"
    - "UAT_RUNBOOK.md documents the manual procedure for each fixture and the round-trip step"
    - "Windows host deploy verified: ConvertToAnalytical button visible in PDA_Tools.tab > Analytical.panel > col1.stack ribbon position; click runs the new script (not a stale cached version)"
  artifacts:
    - path: "~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/script.py"
      provides: "Final _emit_summary function with TaskDialog + Output Window dual surface; main() wired through it"
      contains: "_emit_summary"
    - path: "~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/icon.png"
      provides: "Ribbon icon (engineer-authored binary, visually distinct from ExportToPDA)"
    - path: "~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_minimal_frame.rvt"
      provides: "UAT fixture 1: 4 columns + 2 beams + 1 brace"
    - path: "~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_multi_storey.rvt"
      provides: "UAT fixture 2: 2 storeys, mixed beams + columns; includes one deliberately broken element to exercise missing-section skip path"
    - path: "~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_pre_converted.rvt"
      provides: "UAT fixture 3: same geometry as fixture 1, ConvertToAnalytical already run once -- exercises already-associated path"
    - path: "~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/UAT_RUNBOOK.md"
      provides: "Engineer-readable manual UAT procedure: load each fixture, click button, expected counts, Phase 5 Tier 1 round-trip step"
      contains: "phase07_minimal_frame"
  key_links:
    - from: "main"
      to: "_emit_summary"
      via: "main calls _emit_summary(converted, already, skips) instead of inline TaskDialog"
      pattern: "_emit_summary\\(converted, already, skips\\)"
    - from: "_emit_summary"
      to: "output.linkify"
      via: "skip rows render with clickable element ids in Output Window markdown table (D-08)"
      pattern: "output\\.linkify"
---

<objective>
Close Phase 7 with the diagnostics surface, the UAT fixtures + runbook, and the Windows-host deploy verification.

Three concerns, four task groups (one autonomous code task, three engineer-manual checkpoints):
1. Replace the temporary printf TaskDialog from Plan 7-02's `main()` with the proper `_emit_summary(converted, already, skips)` function: dual surface (TaskDialog always shown per D-09, with distinct already-associated line per D-03; pyRevit Output Window markdown table with `output.linkify(element_id)` clickable links per D-08).
2. Engineer authors three RVT fixtures + a manual UAT runbook in the sibling repo.
3. Engineer deploys the bundle to the Windows host (manual folder copy + pyRevit Reload).
4. Engineer runs the three fixtures + the Phase 5 Tier 1 ExportToPDA round-trip on fixture 1 to honour ROADMAP success criterion 4 / D-13.

This plan closes REVIT-CONVERT-04 (diagnostic output) and provides the operational gates for ROADMAP success criteria 1, 2, 3, 4, 5 (the Tier 1 round-trip is the proof point for #4).

Output:
- Modified `script.py` (with `_emit_summary` and final `main()` wiring).
- New `icon.png` (engineer-authored binary).
- Three new RVT fixtures under `fixtures/phase07/`.
- New `UAT_RUNBOOK.md` documenting the manual procedure.
- Manual deploy + UAT on Windows host -- engineer-confirmed pass per D-12 + D-13.

`autonomous: false` because Tasks 3 and 4 require a Windows host with Revit installed, manual file copy, pyRevit Reload, and engineer-driven button-clicking + visual verification. Task 1 (the `_emit_summary` code edit) is fully autonomous; Task 2 (fixture authoring) is engineer-manual on the Mac side because RVT files are binaries hand-authored in Revit (cannot be code-generated).
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-revit-element-to-analytical-conversion/07-CONTEXT.md
@.planning/phases/07-revit-element-to-analytical-conversion/07-RESEARCH.md
@.planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md
@.planning/phases/07-revit-element-to-analytical-conversion/07-01-SUMMARY.md
@.planning/phases/07-revit-element-to-analytical-conversion/07-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Final public surface (script.py) -->

```python
def _emit_summary(converted, already, skips):
    """D-08 (two-surface) + D-09 (always shown).
    converted: list[ElementId]
    already:   list[ElementId]
    skips:     list[(ElementId, reason: str, role: str|None)]
    Side effects: writes to pyRevit Output Window (markdown table with linkify)
    AND shows a Revit TaskDialog. Both always run, including on a clean run."""
```

`main()` final form (replacing the Plan 7-02 temporary printf):
```python
def main():
    physical_ids = _resolve_input(uidoc, doc)
    if not physical_ids:
        return
    converted, already, skips = run_batch(doc, physical_ids)
    _emit_summary(converted, already, skips)
```

Output API surface used (RESEARCH.md Pattern 7, Code Example 4):
- `output = script.get_output()`
- `output.set_title(str)`
- `output.linkify(ElementId) -> str` (markdown link)
- `output.print_table(table_data: list[list], title: str, columns: list[str])`
- `TaskDialog("title")` instance with `.MainInstruction`, `.MainContent`, `.Show()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add _emit_summary to script.py and rewire main() to call it</name>
  <files>~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/script.py</files>
  <read_first>
    - The current script.py (after Plan 7-02) -- confirm `run_batch` is in place and `main()` currently has the temporary printf TaskDialog with the substring `"converted: {0} | already-associated: {1} | skipped (errors): {2} | total: {3}"`
    - .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md sections M (lines 348-376 -- `_emit_summary` verbatim drop-in), N (lines 401-411 -- main() final shape)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-CONTEXT.md D-08 (two-surface diagnostic), D-09 (always-shown summary), D-03 (already-associated as distinct summary line)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-RESEARCH.md Pitfall 9 (ASCII only -- use `--`, not em-dash), Pattern 7 (lines 266-282 -- linkify + print_table), Code Example 4 (lines 588-615 -- the exact reference implementation)
    - ~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ExportToPDA.pushbutton/script.py lines 477-486 (analog success TaskDialog -- structurally similar but Phase 5 has no Output Window with linkify)
  </read_first>
  <action>
    Insert `_emit_summary` AFTER `run_batch` and BEFORE `main()`. Then REPLACE the `TaskDialog.Show("PDA: Convert to Analytical", summary)` block in `main()` (the Plan 7-02 temporary) with a single call to `_emit_summary(converted, already, skips)`.

    Append:
    ```python
    # -- Diagnostic emission (D-08 dual surface; D-09 always shown) --------------
    def _emit_summary(converted, already, skips):
        """D-08 + D-09. Two surfaces, both always run.

        Surface 1 (Output Window, only if there are skips): markdown table with
        clickable element links via output.linkify; columns: Element, Reason,
        Structural Type. Engineer clicks the link, Revit highlights the element.

        Surface 2 (TaskDialog, always): one-line summary
        'converted: N | already-associated: M | skipped (errors): K | total: T'
        with already-associated as a distinct line per D-03. Body text varies by
        whether there are skips. ASCII-only per Pitfall 9."""
        output = script.get_output()
        output.set_title("PDA: Convert to Analytical")

        if skips:
            rows = [
                [output.linkify(eid), reason, str(role) if role else '-']
                for (eid, reason, role) in skips
            ]
            output.print_table(
                table_data=rows,
                title='Conversion Skips',
                columns=['Element', 'Reason', 'Structural Type'],
            )

        summary = "converted: {0} | already-associated: {1} | skipped (errors): {2} | total: {3}".format(
            len(converted), len(already), len(skips),
            len(converted) + len(already) + len(skips),
        )
        td = TaskDialog("PDA: Convert to Analytical")
        td.MainInstruction = summary
        if skips:
            td.MainContent = "{0} element(s) were skipped. See the pyRevit Output window for clickable links to each.".format(len(skips))
        else:
            td.MainContent = "All elements processed successfully."
        td.Show()
    ```

    REPLACE main() body so the final form is exactly:
    ```python
    def main():
        physical_ids = _resolve_input(uidoc, doc)
        if not physical_ids:
            return
        converted, already, skips = run_batch(doc, physical_ids)
        _emit_summary(converted, already, skips)

    if __name__ == "__main__":
        main()
    ```

    Notes:
    - The Plan 7-02 temporary printf TaskDialog is now gone; remove the inline `summary = "...".format(...)` block and `TaskDialog.Show("PDA: Convert to Analytical", summary)` call from `main()` -- they are subsumed into `_emit_summary`.
    - ALL strings remain ASCII-only (Pitfall 9). Use `--` not em-dash. Use `'` not curly quotes.
    - `output.linkify` accepts an `ElementId` and returns a markdown string. `output.print_table` renders the markdown table in the pyRevit Output Window (RESEARCH.md Pattern 7).
    - The Output Window section is conditional on `skips` being non-empty (no point rendering an empty table). The TaskDialog is unconditional (D-09 -- always shown).
  </action>
  <verify>
    <automated>FILE="$HOME/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/script.py"; grep -q "def _emit_summary" "$FILE" && grep -q "script.get_output()" "$FILE" && grep -q "output.set_title" "$FILE" && grep -q "output.linkify(eid)" "$FILE" && grep -q "output.print_table" "$FILE" && grep -q "td = TaskDialog" "$FILE" && grep -q "td.MainInstruction = summary" "$FILE" && grep -q "td.Show()" "$FILE" && grep -q "_emit_summary(converted, already, skips)" "$FILE" && grep -c 'TaskDialog.Show("PDA: Convert to Analytical"' "$FILE" | awk '{ if ($1 > 0) { print "FAIL: temporary printf TaskDialog.Show with this title still in main(); should be replaced by _emit_summary"; exit 1 } else print "OK temporary TaskDialog.Show absent" }' && ! grep -nP "[^\x00-\x7F]" "$FILE" && echo "OK _emit_summary added; main rewired; ASCII clean"</automated>
  </verify>
  <acceptance_criteria>
    - `_emit_summary(converted, already, skips)` defined; positioned after `run_batch` and before `main()`
    - Calls `script.get_output()` and `output.set_title("PDA: Convert to Analytical")`
    - Conditional on `skips`: builds `rows` via list comprehension that calls `output.linkify(eid)` per row; calls `output.print_table(table_data=rows, title='Conversion Skips', columns=['Element', 'Reason', 'Structural Type'])`
    - Builds `summary` exactly: `"converted: {0} | already-associated: {1} | skipped (errors): {2} | total: {3}"` (D-03 distinct already-associated line; ASCII pipes)
    - Instantiates `TaskDialog("PDA: Convert to Analytical")` with `.MainInstruction = summary`
    - `MainContent` body branches on `skips` truthiness -- informs engineer about Output Window when skips present, "All elements processed successfully." otherwise
    - Calls `td.Show()` unconditionally (D-09)
    - `main()` no longer contains the inline `summary = "..."` printf block from Plan 7-02 -- replaced by `_emit_summary(converted, already, skips)`
    - File ASCII-only (Pitfall 9)
  </acceptance_criteria>
  <done>`_emit_summary` committed; `main()` final form wired through it; temporary printf TaskDialog removed; greps all pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Engineer authors three RVT fixtures + UAT_RUNBOOK.md and commits them to the sibling repo</name>
  <files>
    ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_minimal_frame.rvt,
    ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_multi_storey.rvt,
    ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/phase07_pre_converted.rvt,
    ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/UAT_RUNBOOK.md
  </files>
  <read_first>
    - .planning/phases/07-revit-element-to-analytical-conversion/07-CONTEXT.md D-12 (three RVT fixtures), D-13 (Tier 1 round-trip on fixture 1)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-RESEARCH.md "Validation Architecture" (lines 685-715), "Wave 0 Gaps" (fixtures + runbook expectations), Assumption A6 (negative-path coverage in fixture 2)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md "No Analog Found" (lines 532-541) -- planner directive to treat fixture authoring as engineer-manual
  </read_first>
  <what-built>
    Test fixtures for manual UAT of the ConvertToAnalytical pushbutton:
    - `phase07_minimal_frame.rvt` -- 4 columns + 2 beams + 1 diagonal brace (covers all 3 categories OST_StructuralColumns + OST_StructuralFraming with both Beam and Brace StructuralType)
    - `phase07_multi_storey.rvt` -- 2 storeys, mixed beams + columns; INCLUDES one deliberately broken element (e.g., a column with an unassigned section) to exercise the `missing-section` skip path (RESEARCH.md Assumption A6)
    - `phase07_pre_converted.rvt` -- same geometry as fixture 1, ConvertToAnalytical already run once on it (so re-running on this fixture exercises the `already-associated` path -- D-03 + REVIT-CONVERT-03 + Pitfall 1 idempotency comparison)
    - `UAT_RUNBOOK.md` -- engineer-readable manual procedure for each fixture: load steps, expected TaskDialog counts, expected Output Window content, and the Phase 5 Tier 1 round-trip step (D-13)

    These RVT files are binary, authored in Revit on whichever host the engineer prefers (Mac via Parallels/VM, or directly on the Windows host -- per RESEARCH.md Open Question 4 and Environment Availability table). Cannot be code-generated. Plan-phase explicitly leaves the authoring to the engineer.
  </what-built>
  <action>
    The engineer performs these steps manually, on either Mac (Parallels) or Windows host:

    1. Create `~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/` directory if it does not exist.

    2. Author `phase07_minimal_frame.rvt`:
       - New Revit 2025 structural project (any template).
       - Place 4 structural columns at the corners of a 6m x 4m grid, base level 0, top level 3m.
       - Place 2 structural beams along the long edges connecting the column tops.
       - Place 1 diagonal brace from one column base to the opposite column top.
       - Save as `phase07_minimal_frame.rvt` in the fixtures directory.
       - DO NOT run any analytical conversion -- this fixture starts with zero AnalyticalMembers.

    3. Author `phase07_multi_storey.rvt`:
       - New Revit 2025 structural project.
       - 2 storeys, base level 0, mid level 3m, top level 6m.
       - Columns at 4 corners on both storeys.
       - Beams at storey 1 perimeter and storey 2 perimeter (8 beams total).
       - Deliberately break ONE element: pick one column, set its `Structural Material` parameter to `<None>` (or assign a family that has no Section). This is what exercises the `missing-section` skip path.
       - Save as `phase07_multi_storey.rvt`.

    4. Author `phase07_pre_converted.rvt`:
       - Open `phase07_minimal_frame.rvt` (or recreate the same geometry).
       - Run Revit's UI-side "Analytical Automation" or this very ConvertToAnalytical pushbutton ONCE so all 7 elements have associated analytical members.
       - Save As `phase07_pre_converted.rvt`.
       - Verify in the analytical model browser that 7 AnalyticalMembers exist with 1:1 physical associations.

    5. Author `UAT_RUNBOOK.md` in the same `fixtures/phase07/` directory. The runbook must include, at minimum:
       - One H2 section per fixture (`## Fixture 1: phase07_minimal_frame.rvt`, etc.)
       - For each fixture: steps to open in Revit 2025, what to select, what to click, expected TaskDialog summary line (e.g., for fixture 1: `converted: 7 | already-associated: 0 | skipped (errors): 0 | total: 7`)
       - For fixture 2: expected `skipped (errors): 1` with `missing-section` reason in the Output Window markdown table; engineer confirms by clicking the linkify link and seeing Revit highlight the broken column
       - For fixture 3: expected `converted: 0 | already-associated: 7 | skipped (errors): 0 | total: 7` (D-03 idempotency)
       - One H2 section `## Tier 1 Round-Trip (D-13, ROADMAP success criterion 4)` documenting: open fixture 1; run ConvertToAnalytical; switch to a drafting view containing detail lines representing the analytical model OR (if no detail lines authored) author them by tracing the analytical members; run the existing Phase 5 `ExportToPDA` pushbutton; save the resulting JSON; load in the frame2d browser UI; solve; confirm reactions match the analytical reference within tolerance (use the same tolerance Phase 5/4 UAT used)

    6. Commit all four files (3 RVT + 1 MD) to the sibling `CustomRevitExtension` repo with a single commit.

    Note: this task's "action" requires manual authoring of binary RVT files in Revit -- there is no CLI/API path. The engineer is the executor. Acceptance: all four files present at the canonical paths in the sibling repo, the runbook documents the procedure for each fixture (including the deliberately broken column in fixture 2), and the engineer has saved + committed them.
  </action>
  <verify>
    <automated>DIR="$HOME/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07"; test -d "$DIR" && test -f "$DIR/phase07_minimal_frame.rvt" && test -f "$DIR/phase07_multi_storey.rvt" && test -f "$DIR/phase07_pre_converted.rvt" && test -f "$DIR/UAT_RUNBOOK.md" && grep -q "phase07_minimal_frame" "$DIR/UAT_RUNBOOK.md" && grep -q "phase07_multi_storey" "$DIR/UAT_RUNBOOK.md" && grep -q "phase07_pre_converted" "$DIR/UAT_RUNBOOK.md" && grep -qi "tier 1\|round-trip\|round trip" "$DIR/UAT_RUNBOOK.md" && echo "OK fixture directory and runbook present"</automated>
  </verify>
  <acceptance_criteria>
    - `~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/` directory exists
    - `phase07_minimal_frame.rvt` exists and contains exactly 4 columns + 2 beams + 1 brace (engineer visually confirms in Revit; document confirmation in commit message)
    - `phase07_multi_storey.rvt` exists; contains a 2-storey frame with one element deliberately broken (engineer documents which element in the runbook)
    - `phase07_pre_converted.rvt` exists; opening it shows 7 AnalyticalMembers already associated to the 7 physical elements
    - `UAT_RUNBOOK.md` exists at `fixtures/phase07/UAT_RUNBOOK.md`
    - Runbook contains H2 sections for each of the 3 fixtures plus one for the Tier 1 round-trip
    - Runbook documents expected TaskDialog summary line per fixture (correct counts)
    - All four files committed to the sibling repo (engineer confirms with `git log` showing the commit)
  </acceptance_criteria>
  <done>Three RVT fixtures + UAT_RUNBOOK.md exist at canonical sibling-repo paths; runbook content satisfies the grep checks; engineer has committed them with a single commit and shared the SHA.</done>
  <resume-signal>Type "fixtures committed" with the sibling-repo commit SHA, or describe issues authoring any fixture.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Engineer authors icon.png and deploys the bundle to the Windows host (manual folder copy + pyRevit Reload)</name>
  <files>
    ~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/icon.png
  </files>
  <read_first>
    - .planning/phases/07-revit-element-to-analytical-conversion/07-CONTEXT.md (canonical refs section -- sibling repo path)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-RESEARCH.md Pitfall 7 (stale Windows-host bundle), Open Question 4 (col1.stack ordering)
    - .planning/STATE.md "truss-json-solver-mismatch" debug session entry (the 2026-04-24 prior incident this task explicitly defends against)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-PATTERNS.md icon.png section (lines 432-440)
    - Memory: `revit_host_manual_copy_deployment.md`, `pyrevit_stack_column_limit.md`
  </read_first>
  <what-built>
    Two deliverables in this task:

    (a) `icon.png` for the new pushbutton -- engineer authors a square ribbon icon (32x32 or 64x64), visually distinct from `ExportToPDA.pushbutton/icon.png`, and commits it to the sibling repo at the canonical pushbutton path.

    (b) Windows-host deployment of the `ConvertToAnalytical.pushbutton/` folder so the new button appears in the Revit ribbon. Per project memory (`revit_host_manual_copy_deployment.md` + STATE.md debug session 2026-04-24 "truss-json-solver-mismatch"), the Windows host is NOT a git clone of the sibling repo. New pushbuttons require explicit folder copy.
  </what-built>
  <action>
    Step A -- icon.png authoring (Mac side):

    1. Engineer creates a square PNG ribbon icon (32x32 or 64x64; pyRevit auto-scales).
    2. Visually distinct from `ExportToPDA.pushbutton/icon.png` so the two buttons don't blur together at ribbon size.
    3. Save as `icon.png` in `~/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/icon.png`.
    4. Commit to sibling repo with the existing Phase 7 commits (or as a new commit -- engineer's call).

    Step B -- Windows-host deploy:

    1. From the Mac, push the latest sibling-repo commits (Plan 7-01 + 7-02 + 7-03 work, including icon.png) to the remote.

    2. On the Windows host, obtain the new files. EITHER:
       - (a) `git pull` in the Windows checkout of `CustomRevitExtension` (if the engineer maintains one), then copy the relevant files; OR
       - (b) Download `bundle.yaml`, `script.py`, and `icon.png` directly from `raw.githubusercontent.com` per the 2026-04-24 debug-session pattern.

    3. Copy the entire `ConvertToAnalytical.pushbutton/` folder (with all three files) into the Windows host's pyRevit extensions path:
       ```
       %APPDATA%\pyRevit\Extensions\PDA_customRevit.extension\PDA_Tools.tab\Analytical.panel\col1.stack\
       ```
       The folder name MUST be `ConvertToAnalytical.pushbutton` exactly (pyRevit treats the folder name as button identity).

    4. In Revit 2025 with any document open, run pyRevit's "Reload" command (or restart Revit).

    5. Visual verification on the ribbon:
       - Open `PDA_Tools` tab > `Analytical` panel > `col1` stack
       - The new `Convert to Analytical` button is visible. The two-line title `Convert to / Analytical` mirrors the existing `Export to / PDA` button two-line convention
       - The icon is the new `icon.png` (visually distinct from `Export to PDA`)
       - Tooltip on hover matches the `bundle.yaml` tooltip text: "Convert selected physical structural columns, beams, and bracings into AnalyticalMember instances. Idempotent -- already-associated elements are skipped."

    6. Cache check: per Pitfall 7 (RESEARCH.md), pyRevit may keep a stale `.pyc` cached form of an old script under the same path. If the button appears but does not run the new code, force a clean reload by:
       - Closing Revit
       - Deleting any `.pyc` files in the `ConvertToAnalytical.pushbutton/` folder on the Windows host
       - Reopening Revit and pyRevit Reload

    7. Smoke click: open `phase07_minimal_frame.rvt` (Fixture 1), select all 7 physical elements, click the button. Engineer confirms a TaskDialog appears within 1-2 seconds reporting nonzero `converted` count.

    NOTE on stack column count (per `pyrevit_stack_column_limit.md` memory + RESEARCH.md Open Question 4): `col1.stack/` already has 5 buttons. With Phase 7's addition that becomes 6. pyRevit should auto-flow to a second stack row. If the engineer sees the new button in an unexpected ribbon position OR observes overflow oddities, the resolution is to add a `col2.stack/` directory and move the new button there. That decision is the engineer's at deploy time -- do NOT pre-author `col2.stack/`.
  </action>
  <verify>
    <automated>ICON="$HOME/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ConvertToAnalytical.pushbutton/icon.png"; test -f "$ICON" && file "$ICON" | grep -qi "PNG image" && echo "OK icon.png present and valid PNG (Mac side)"; echo "Windows-side deploy + smoke-click verification is engineer-manual -- see resume-signal"</automated>
  </verify>
  <acceptance_criteria>
    - `icon.png` exists at the canonical pushbutton path on the Mac sibling repo and is a valid PNG
    - The folder `%APPDATA%\pyRevit\Extensions\PDA_customRevit.extension\PDA_Tools.tab\Analytical.panel\col1.stack\ConvertToAnalytical.pushbutton\` exists on the Windows host with all three files (`bundle.yaml`, `script.py`, `icon.png`)
    - pyRevit Reload completes without errors in the pyRevit log
    - In Revit 2025, the new button is visible in the `PDA_Tools` ribbon tab under `Analytical` panel
    - Hovering shows the tooltip from `bundle.yaml`
    - Smoke-click on fixture 1 produces a TaskDialog within 1-2 seconds with nonzero `converted` count -- proving the button runs the new code, not a stale cache
    - Engineer confirms no `.pyc` cache anomaly (Pitfall 7)
  </acceptance_criteria>
  <done>icon.png committed to sibling repo (Mac); bundle deployed to Windows host; ribbon button visible and clickable; smoke click runs the new code per the engineer's resume signal.</done>
  <resume-signal>Type "deploy verified" with a screenshot of the ribbon button + TaskDialog, OR describe what failed (stale cache, ribbon position issue, missing button, etc.).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Engineer runs the three UAT fixtures + the Phase 5 Tier 1 round-trip; confirms all five ROADMAP success criteria</name>
  <files>
    ~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/UAT_RUNBOOK.md
  </files>
  <read_first>
    - `~/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/UAT_RUNBOOK.md` (engineer-authored in Task 2 -- this is the canonical procedure)
    - .planning/ROADMAP.md "Phase 7" Success Criteria (5 numbered criteria -- this UAT proves them all)
    - .planning/phases/07-revit-element-to-analytical-conversion/07-CONTEXT.md D-12, D-13
    - .planning/phases/07-revit-element-to-analytical-conversion/07-RESEARCH.md "Phase Requirements -> Verification Map" (lines 696-703)
  </read_first>
  <what-built>
    Manual UAT pass against the Phase 7 deliverable on the Windows host. Verifies all five ROADMAP success criteria for Phase 7 (the gate to mark Phase 7 complete).
  </what-built>
  <action>
    Engineer follows the procedures in `UAT_RUNBOOK.md` (committed in Task 2) on the Windows host (per Task 3 deploy):

    **Fixture 1 -- `phase07_minimal_frame.rvt` (REVIT-CONVERT-01 + REVIT-CONVERT-02; ROADMAP success criterion 1):**
    1. Open the fixture.
    2. Click the `Convert to Analytical` button.
    3. Use the PickObjects prompt to select all 4 columns + 2 beams + 1 brace = 7 elements; press Finish.
    4. EXPECT TaskDialog: `converted: 7 | already-associated: 0 | skipped (errors): 0 | total: 7`. Body reads "All elements processed successfully."
    5. Open the analytical model browser. Verify 7 AnalyticalMember instances with 1:1 physical associations.
    6. Inspect each AnalyticalMember's properties: confirm `SectionTypeId` and `MaterialId` are non-null (D-10 read-back actually preserves section/material via AddAssociation -- D-11 reversal proves out).
    7. Press Ctrl+Z (Undo) ONCE. EXPECT all 7 AnalyticalMembers to disappear in a single undo step labelled "PDA: Convert to Analytical" (Pitfall 5 -- Assimilate, not Commit).
    8. Press Ctrl+Y (Redo) once -- AnalyticalMembers reappear.

    **Fixture 2 -- `phase07_multi_storey.rvt` (REVIT-CONVERT-04; ROADMAP success criterion 3):**
    1. Open the fixture.
    2. Click the button. Select all physical structural elements (engineer counts -- documented in runbook).
    3. EXPECT TaskDialog: `converted: N-1 | already-associated: 0 | skipped (errors): 1 | total: N` where N is the total selection count.
    4. EXPECT pyRevit Output Window to show a markdown table titled "Conversion Skips" with one row: clickable element link | `missing-section` | structural-type-name (e.g., `Column`).
    5. Click the linkify link in the Output Window. EXPECT Revit to highlight (zoom-to-element) the deliberately broken column from Fixture 2 authoring.

    **Fixture 3 -- `phase07_pre_converted.rvt` (REVIT-CONVERT-03; ROADMAP success criterion 2):**
    1. Open the fixture (which already has 7 AnalyticalMembers from prior conversion).
    2. Click the button. Select the same 7 physical elements.
    3. EXPECT TaskDialog: `converted: 0 | already-associated: 7 | skipped (errors): 0 | total: 7`.
    4. Verify document AnalyticalMember count is unchanged (still 7) -- no duplicates.
    5. Confirm the `already-associated` count is on its own distinct line in the summary, not bundled with skipped errors (D-03).

    **Tier 1 round-trip on Fixture 1 (D-13; ROADMAP success criterion 4):**
    1. Reopen `phase07_minimal_frame.rvt` (or use Fixture 3's already-converted state).
    2. Switch to a drafting view containing detail-line representations of the model (engineer authors per UAT_RUNBOOK.md if not already present).
    3. Run the existing `Export to PDA` pushbutton (Phase 5 Tier 1 frame exporter) on that drafting view. Save the canonical PDA JSON to a known path.
    4. Open the frame2d browser UI (or POST the JSON to `/solve/frame2d` directly).
    5. Solve. Compare reactions to the analytical reference for the minimal-frame geometry (use the same tolerance Phase 5/4 UAT used).
    6. EXPECT pass: round-trip values match within tolerance. This proves the analytical model produced by Phase 7's ConvertToAnalytical is well-formed enough for downstream tooling (specifically, for Phase 8 Tier 2 export to consume in the next phase).

    **Configurable category filter (REVIT-CONVERT-01; ROADMAP success criterion 5):**
    1. Visually inspect script.py top of file. Confirm `SUPPORTED_CATEGORIES` is a module-level dict literal -- visually it looks extensible (engineer comments confirm the v1.5+ Phase 15 path mentioned in code comments).
    2. Mental walkthrough (no code change yet): engineer confirms that adding a new key (e.g., `BuiltInCategory.OST_Floors: convert_slab`) plus a new handler function would extend support without touching the call site. This is the configurability proof; no actual extension is performed in Phase 7.

    **Final confirmation:** Engineer signs off in `UAT_RUNBOOK.md` (or in the resume signal) recording each fixture's pass/fail and the round-trip pass/fail. If all green, Phase 7 is complete.
  </action>
  <verify>
    <automated>RUNBOOK="$HOME/Documents/CustomRevitExtension/PDA_customRevit.extension/fixtures/phase07/UAT_RUNBOOK.md"; test -f "$RUNBOOK" && echo "OK runbook present (engineer signs off in resume-signal -- automated check is presence of the runbook only; the actual UAT is engineer-driven on the Windows host)"</automated>
  </verify>
  <acceptance_criteria>
    - Fixture 1: TaskDialog reports `converted: 7 | already-associated: 0 | skipped (errors): 0 | total: 7`; analytical browser shows 7 AnalyticalMembers with non-null SectionTypeId + MaterialId; Ctrl+Z reverts all 7 in ONE undo step labelled "PDA: Convert to Analytical"
    - Fixture 2: TaskDialog reports nonzero skipped count (>=1); Output Window markdown table contains at least one row with `missing-section` reason; clicking the link in the table highlights the offending element in Revit
    - Fixture 3: TaskDialog reports `converted: 0 | already-associated: 7 | skipped (errors): 0 | total: 7`; document AnalyticalMember count unchanged at 7
    - Tier 1 round-trip on Fixture 1: Phase 5 ExportToPDA produces JSON; frame2d UI loads it; /solve/frame2d returns reactions matching analytical reference within tolerance
    - Configurable filter (REVIT-CONVERT-01 success criterion 5): engineer confirms SUPPORTED_CATEGORIES dict is the configuration surface; mental walkthrough confirms v1.5+ Phase 15 extension would not require call-site rewiring
    - Engineer signs off in `UAT_RUNBOOK.md` (or in commit message + resume signal)
  </acceptance_criteria>
  <done>All 5 ROADMAP success criteria green per engineer's resume signal; sign-off captured in UAT_RUNBOOK.md or commit history.</done>
  <resume-signal>Type "Phase 7 UAT pass -- all 5 criteria green" with summary of each fixture's actual TaskDialog text, OR list which criteria failed and the specific failure mode (e.g., "fixture 3 idempotency: TaskDialog showed converted=7 instead of 0 -- duplicates created").</resume-signal>
</task>

</tasks>

<verification>
- Task 1 verification: `_emit_summary` present in script.py; main() calls it; ASCII-clean; no leftover Plan 7-02 placeholder TaskDialog with the inline summary string in main().
- Task 2 verification: 4 binary/markdown files committed at the sibling repo's `fixtures/phase07/` path; runbook documents the procedure.
- Task 3 verification: icon.png committed (Mac); Windows host has the bundle folder; pyRevit Reload succeeded; ribbon shows the new button; smoke click runs the new code.
- Task 4 verification: All 5 ROADMAP success criteria pass on the Windows host with the 3 fixtures + Tier 1 round-trip.
</verification>

<success_criteria>
Phase 7 closes when all four tasks are complete:
- _emit_summary committed (autonomous Task 1).
- Three fixtures + UAT_RUNBOOK.md committed to the sibling repo (manual Task 2).
- icon.png committed + Windows host deploy verified (manual Task 3).
- All three fixtures + Tier 1 round-trip pass on the Windows host (manual Task 4).

Specifically REVIT-CONVERT-04 fully satisfied (D-08 dual-surface diagnostic + D-09 always-shown). Combined with Plan 7-01 (REVIT-CONVERT-01 surface) and Plan 7-02 (REVIT-CONVERT-02 fully via D-11 reversed + REVIT-CONVERT-03), Phase 7's full requirement set is closed.

ROADMAP success criteria:
1. Engineer-clickable conversion (Fixture 1 pass)
2. Idempotent re-run (Fixture 3 pass)
3. Final TaskDialog summary with per-skip reasons; one bad element doesn't abort (Fixture 2 pass)
4. Phase 5/v1.2 Tier 1 round-trip on Fixture 1 (round-trip pass)
5. Configurable category filter (SUPPORTED_CATEGORIES dict)
</success_criteria>

<output>
After completion, create `.planning/phases/07-revit-element-to-analytical-conversion/07-03-SUMMARY.md` documenting:
- `_emit_summary` added; main() final form (calls `_emit_summary`)
- Three RVT fixtures committed (paths + commit SHA)
- UAT_RUNBOOK.md committed (path + commit SHA)
- icon.png committed (path + commit SHA)
- Windows host deploy verification: button visible, smoke click pass; resolution path if `col1.stack/` overflowed (e.g., engineer added `col2.stack/`)
- UAT outcomes per fixture: fixture 1 (7 converted, 0 skipped, undo reverts in 1 step); fixture 2 (>=1 skipped with `missing-section`, linkify works); fixture 3 (0 converted, 7 already-associated, no duplicates)
- Tier 1 round-trip outcome on Fixture 1 (pass / values within tolerance)
- Phase 7 status: complete; ready for Phase 8 entry (Tier 2 export)
- Note: REVIT-CONVERT-02 row in REQUIREMENTS.md should be updated to reflect that the manual `AnalyticalMember.Create` + `AddAssociation` path is the IMPLEMENTED path (D-11 reversal); the original "primary"/"fallback" framing was based on an incorrect API assumption (`Document.GenerateMembersFromSelection` does not exist as a public API method)
</output>
