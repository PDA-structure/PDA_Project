---
phase: 999.2-load-combination-generator
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - ui/truss2d/script.js
  - ui/truss2d/index.html
  - ui/truss2d/style.css
autonomous: false
requirements: [D-18, D-19, D-20, D-21]

must_haves:
  truths:
    - "A results-view selector [per-case] / [per-combination] / [ENVELOPE] sits atop the results panel; ENVELOPE is default when combinations exist, else per-case"
    - "The ENVELOPE view shows worst force per member plus a Governing combination column (forward map, D-18/D-19)"
    - "Clicking a combination row in the combination table highlights the members it governs on the canvas with the spike-yellow governing halo (reverse index, D-19)"
    - "exportAnalysis writes a per-member load_combination governing string into each tension/compression row, keeps the top-level load_combination, and bumps schema_version to 1.2 (additive, non-breaking)"
    - "When no combinations exist, results and export behave as today (back-compat)"
  artifacts:
    - path: "ui/truss2d/script.js"
      provides: "results-view selector, governing column + member-detail strip, canvas governing highlight, per-member export string, schema 1.2"
      contains: "governing"
    - path: "ui/truss2d/index.html"
      provides: "results-view segmented selector at top of resultsPanel + Governing column header"
      contains: "resultsViewSelector"
    - path: "ui/truss2d/style.css"
      provides: "--canvas-governing token (spike yellow halo) + segmented selector + detail strip styles"
      contains: "canvas-governing"
  key_links:
    - from: "ui/truss2d/script.js renderResults"
      to: "envelope (from recombine) governing combo per member"
      via: "ENVELOPE view reads the cached envelope and adds a Governing column"
      pattern: "gov_tension|governing"
    - from: "ui/truss2d/script.js exportAnalysis (~line 1443)"
      to: "per-member load_combination string"
      via: "rows.push adds load_combination; schema_version bumped to 1.2"
      pattern: "load_combination"
---

<objective>
Surface the governing-combination traceability (THE differentiator, D-18/D-19) in the truss2d
results UI and carry it into the export contract (D-21). Adds the [per-case]/[per-combination]/
[ENVELOPE] results selector (D-20), a Governing-combination column + per-member detail strip
(forward map), a canvas reverse-index highlight (combo -> members it governs), and the per-member
governing-combo string in exportAnalysis (schema 1.1 -> 1.2, additive, non-breaking for the
SEED-005 marimo reader).

Purpose: Designs out the Tekla pain — engineers see WHICH combination governs each member, in both
directions, and that provenance flows to the EC3 tension sheet via the existing export seam.

Output: modifications to ui/truss2d/{script.js, index.html, style.css}.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/catrinevans/Documents/pda_project/CLAUDE.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-UI-SPEC.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-PATTERNS.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-RESEARCH.md

<interfaces>
Verified line references in ui/truss2d/ this session:
- `function renderResults(res)` at script.js:1699 — tension/compression/reaction/displacement tables; delta_max summary at ~1757-1768 (drives the SLS check, D-13).
- `function exportAnalysis(mode)` at script.js:1425 — top-level load_combination string at ~1428; tension/compression rows.push at ~1443; written at `schema_version: '1.1'` at 1544; package-level `load_combination` at ~1547.
- resultsPanel at index.html:265.
From Plan 04: the `combinations` model, `perCaseForces` cache, `superposeJS`, `envelopeJS`, `recombine()` (which stores the latest envelope), and the combination table with selectable rows.
UI-SPEC C6/C7/C8 contracts; --canvas-governing = spike-primary yellow #F5C82E halo (its only canvas use; SOLVE is the only other yellow). Governing column truncates long expressions with hover-tooltip full string.
SEED-005 reader binds to tension_members[].force_kN + A_cm2 + top-level load_combination — the per-member field is ADDITIVE; do not remove or rename existing fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Results-view selector (C6) + Governing column/detail (C7) + canvas highlight (C8)</name>
  <read_first>
    - ui/truss2d/script.js lines 1699-1781 (renderResults; tension/compression tables; delta_max), 589-700 (draw for the canvas highlight)
    - ui/truss2d/index.html lines 256-280 (resultsPanel)
    - ui/truss2d/style.css (table styles, --color-accent-bright, .tool-btn segmented pattern)
    - 999.2-UI-SPEC.md sections "C6 — Results-view selector + C7/C8 traceability contract", "Color" (--canvas-governing, accent reserved list), Copywriting Contract (empty-state copy)
    - CONTEXT D-18, D-19, D-20
  </read_first>
  <action>
index.html — at the top of #resultsPanel (line 265), add a segmented selector
`#resultsViewSelector` with three .tool-btn-style segments labelled per-case / per-combination /
ENVELOPE; active segment uses --color-accent-bright. Add an optional `<select>` that appears for
per-case (pick a case) and per-combination (pick a combination). Add a "Governing combination"
`<th>` to the tension and compression result tables (shown only in ENVELOPE view).

style.css — add the `--canvas-governing` token = #F5C82E (same in dark) in :root and
[data-theme="dark"]; add a segmented-control style (active = accent-bright) and a `.member-detail`
strip style reusing existing card/border tokens. The governing column cell truncates with
ellipsis + a title attribute carrying the full expression (hover tooltip).

script.js — extend renderResults(res) (script.js:1699) to honour the active results view:
- per-case: a case dropdown; tables show that case's raw forces from perCaseForces (today's
  behaviour scoped to a case).
- per-combination: a combination dropdown; tables show that combination's superposeJS forces.
- ENVELOPE (default when combinations exist; else per-case for back-compat): tables show the worst
  force per member from the cached envelope (from recombine()), one row per member (NOT one row per
  combination — the legibility-at-scale requirement), with the Governing combination column filled
  from the member's gov_tension (and gov_compression available in the detail strip).
Forward map (D-19): clicking a member result row (or a canvas member) opens a `.member-detail`
strip listing that member's force in EVERY on combination with the governing one flagged; the
governing expression shown is exactly what the export will write (D-21).
Reverse index (D-19 / C8): clicking a combination row in the Wave-4 table selects it and, in
draw() (script.js:589), highlights every member in that combo's combo_to_members with a thick
semi-transparent --canvas-governing halo; selecting none clears the highlight. Wire a
`selectedComboName` state used by draw().
Empty-state copy (UI-SPEC): ENVELOPE view with no combinations shows "Run combinations to see which
one governs each member."
  </action>
  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && node --check ui/truss2d/script.js && grep -q "resultsViewSelector" ui/truss2d/index.html && grep -q "canvas-governing" ui/truss2d/style.css && grep -qi "governing" ui/truss2d/script.js && grep -q "selectedComboName" ui/truss2d/script.js && grep -q "member-detail" ui/truss2d/style.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - node --check ui/truss2d/script.js exits 0
    - index.html contains resultsViewSelector and a Governing combination column header
    - style.css contains --canvas-governing (#F5C82E) in :root and [data-theme="dark"], plus a .member-detail style
    - script.js contains selectedComboName and reads it in draw() to paint the governing halo
    - ENVELOPE view renders one row per member with a populated Governing column; per-case/per-combination views switch the displayed forces
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Results selector switches per-case/per-combination/ENVELOPE; ENVELOPE shows governing combo per member with a member-detail breakdown; selecting a combo highlights governed members on the canvas.</done>
</task>

<task type="auto">
  <name>Task 2: Per-member governing-combo export string + schema 1.2 (D-21, non-breaking)</name>
  <read_first>
    - ui/truss2d/script.js lines 1425-1589 (exportAnalysis: top-level load_combination ~1428, rows.push ~1443, schema_version '1.1' at 1544, package load_combination ~1547)
    - 999.2-RESEARCH.md section "Export contract change (D-21)" (the exact additive change + reader impact)
    - 999.2-PATTERNS.md section "(f) Export string change (D-21)"
    - CONTEXT D-21 and canonical_refs (SEED-005 handoff contract)
  </read_first>
  <action>
In exportAnalysis() (script.js:1425), inside the tension/compression `rows.push({...})` (~1443),
add an ADDITIVE field `load_combination: governingComboFor(idx)` where `governingComboFor(memberIndex)`
returns the member's governing-combination string from the cached envelope (the gov_tension
expression for tension rows; gov_compression for compression rows). When no combinations exist,
`governingComboFor` returns the existing top-level LOAD_COMBINATION string so old behaviour is
preserved.

Keep the top-level `load_combination` field (~1428/1547) present for back-compat; when an envelope
exists set it to a summary like "ULS/SLS envelope — see per-member"; otherwise keep the legacy
string. Bump `schema_version` '1.1' -> '1.2' at line 1544 (additive — the SEED-005 marimo reader
binds to force_kN/A_cm2/top-level load_combination, all unchanged; per-member load_combination is
a new optional enrichment the reader can opt into). Do NOT remove or rename any existing export
field.
  </action>
  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && node --check ui/truss2d/script.js && grep -q "governingComboFor" ui/truss2d/script.js && grep -q "schema_version: '1.2'" ui/truss2d/script.js && grep -c "load_combination" ui/truss2d/script.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - node --check ui/truss2d/script.js exits 0
    - script.js contains governingComboFor and `schema_version: '1.2'`
    - per-member rows include a load_combination field (grep shows load_combination count increased vs the single top-level occurrence)
    - top-level load_combination is still present (back-compat); no existing export field removed/renamed
    - With zero combinations, exportAnalysis still produces the legacy shape (top-level string preserved)
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Each exported member row carries its governing-combination string; top-level field retained; schema bumped to 1.2 additively — the marimo reader is unaffected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — traceability + export round-trip</name>
  <action>Automation for this plan completed in the prior tasks; the executor now pauses for the human to run the browser UAT steps below against the live launchd uvicorn service and confirm each behaves as described. No code is written in this task — it is a verification gate.</action>
  <what-built>
    Results-view selector (per-case/per-combination/ENVELOPE), governing-combination column +
    member-detail strip, canvas reverse-index highlight, and the per-member governing-combo export
    string at schema 1.2.
  </what-built>
  <how-to-verify>
    1. Restart the launchd uvicorn service, open the truss2d UI, build a truss with Dead + Imposed
       + Wind cases (Wave-3) and generate combinations (Wave-4), then SOLVE / run combinations.
    2. Confirm the results panel defaults to ENVELOPE and shows one row per member with a Governing
       combination column; switch to per-case and per-combination and confirm the forces change to
       the selected case/combination.
    3. Click a member row — confirm the member-detail strip lists that member's force in every
       combination with the governing one flagged.
    4. Click a combination row in the Combinations table — confirm the members it governs light up
       on the canvas with the yellow governing halo; deselect clears it. Confirm SOLVE is still the
       only other yellow element.
    5. Export the analysis JSON — open it and confirm schema_version is "1.2", each
       tension_members/compression_members row has a load_combination string (the governing combo),
       and the top-level load_combination is still present.
    6. Back-compat: with NO combinations defined, export and confirm the legacy shape (top-level
       load_combination present, results behave as before).
  </how-to-verify>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
    <manual>Human confirms every how-to-verify step passes in the browser.</manual>
  </verify>
  <done>Human has exercised all how-to-verify steps in the browser and replied "approved".</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI export → downstream SEED-005 marimo reader | The exported JSON crosses into a separate consumer; the additive schema bump must not break the existing binding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9992-14 | Tampering | schema change breaking the marimo reader contract | mitigate | strictly additive: per-member load_combination added, top-level retained, force_kN/A_cm2 untouched; schema_version bumped to 1.2 so the reader can detect the richer shape (RESEARCH Export section) |
| T-9992-15 | Logic | governingComboFor called with no envelope (zero combinations) | mitigate | falls back to the existing top-level LOAD_COMBINATION string; export keeps the legacy shape |
</threat_model>

<verification>
- `node --check ui/truss2d/script.js` exits 0
- grep checks in both implementation tasks pass
- Browser UAT (Task 3) approved: selector + governing column + detail strip + canvas halo work; export at schema 1.2 carries per-member governing strings and retains the top-level field; back-compat confirmed
</verification>

<success_criteria>
- [per-case]/[per-combination]/[ENVELOPE] selector works; ENVELOPE shows governing combo per member (D-18/D-20)
- Forward map (member -> all combos) + reverse index (combo -> members, canvas halo) both work (D-19)
- Export carries per-member governing strings at schema 1.2, non-breaking for the marimo reader (D-21)
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-load-combination-generator/999.2-05-SUMMARY.md`
</output>
