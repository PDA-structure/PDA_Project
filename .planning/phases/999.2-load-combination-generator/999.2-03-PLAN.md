---
phase: 999.2-load-combination-generator
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - ui/truss2d/script.js
  - ui/truss2d/index.html
  - ui/truss2d/style.css
autonomous: false
requirements: [D-02, D-03, D-22]

must_haves:
  truths:
    - "A default Dead load case always exists at init; the quick-solve path (solve()) works identically whether or not the Load-Case panel was ever opened"
    - "Every load carries a caseId tagging it to the active case; new loads tag to the active case"
    - "A Load-Case panel (table + active-case selector + add/rename/set-nature) appears above Material Properties and can float via setupCardFloat"
    - "Imposed-nature cases expose a category selector (A–D/H default, E storage); non-imposed cases do not; the category is carried in the case data model and saved/loaded"
    - "Loads can be coloured by nature on the canvas, gated behind a Display checkbox; legacy untyped saved models load all loads into the default Dead case"
    - "loadCases and caseId tags save and load via the interchange JSON additively, with byte-compatible behaviour for legacy files"
  artifacts:
    - path: "ui/truss2d/script.js"
      provides: "loadCases model (with imposed category), activeCaseId, default Dead case, per-case tagging, colour-by-nature, backward-compat load, save extension"
      contains: "activeCaseId"
    - path: "ui/truss2d/index.html"
      provides: "Load-Case panel section (C1/C2) + Display toggle for colour-by-nature"
      contains: "Load Cases"
    - path: "ui/truss2d/style.css"
      provides: "canvas-nature tokens (light+dark) + case-row styles"
      contains: "canvas-nature"
  key_links:
    - from: "ui/truss2d/script.js load entry (~line 257)"
      to: "activeCaseId"
      via: "loads.push tags caseId = activeCaseId"
      pattern: "caseId"
    - from: "ui/truss2d/script.js loadModelFromJson (~line 1640)"
      to: "default Dead case"
      via: "legacy loads without caseId default to Dead (mirror memberOverrides default-on-read)"
      pattern: "loadCases"
---

<objective>
Add the typed load-case layer to the truss2d browser UI (CONTEXT "Concept 1"): a JS load-case
data model with an always-present default Dead case, per-case load tagging, a Load-Case panel
(table + active-case selector + an imposed-category selector), colour-by-nature canvas display
gated behind a Display checkbox, and additive save/load with legacy backward-compatibility.

Purpose: Establishes the case model the combination generator (Wave 4) and results traceability
(Wave 5) build on, WITHOUT touching the quick-solve path (D-03). Carries the imposed ψ0 category
(A–D/H default, E storage) the engine needs for correct accompanying factors. Built portably
(D-22) so the same model transfers to frame2d.

Output: modifications to ui/truss2d/{script.js, index.html, style.css}.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/catrinevans/Documents/pda_project/CLAUDE.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-UI-SPEC.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-PATTERNS.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-RESEARCH.md

<interfaces>
Verified line references in ui/truss2d/ this session:
- `let loads = [];` at script.js:159 — load objects are {nodeId, direction, magnitude}; add caseId.
- Load entry: `loads.push({ nodeId: n.id, direction: dir.toLowerCase(), magnitude: mag });` at script.js:257 — add `caseId: activeCaseId`.
- resetAll clears arrays at script.js:404 (`loads = [];`) — must re-create the default Dead case here too.
- `async function solve()` at script.js:514 — quick path; MUST stay working with the active case.
- `function draw()` at script.js:589 — canvas render; add colour-by-nature here gated on the new checkbox.
- `function saveModel()` at script.js:1327; `schema_version: "1.0"` at 1365; memberOverrides additive write at 1381 — mirror for loadCases.
- loadModelFromJson legacy default-on-read at script.js:1640 (`if (data.canvas && data.canvas.memberOverrides)`) — mirror for loadCases default-to-Dead.
- `setupCardFloat()` at script.js:1786 auto-injects the float button into every .panel-section.
- index.html: Material Properties section at line 202 (insert Load-Case panel directly above); Display toggles at line 212 (chkGrid/chkSupports/chkLoads use onchange="draw()").
- canvas-nature token values to add (UI-SPEC Color): Self weight #78909c/#90a4ae, Dead #455a64/#b0bec5, Imposed #b9770e/#e0a93b, Wind #1f8fb5/#4db6d6 (light/dark).
- Imposed category options (engine PSI0_IMPOSED keys, Plan 01): "A-D/H" (default, label "A–D/H (offices/floors)") and "E_storage" (label "E (storage)"). The category posts to the Wave-2 endpoint as `case.category`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JS load-case data model (+ imposed category) + tagging + backward-compat + save/load</name>
  <read_first>
    - ui/truss2d/script.js lines 150-260, 400-410, 514-590, 1327-1395, 1595-1670
    - 999.2-PATTERNS.md sections "(c) Load-entry tagging", "(g) Backward-compat load", "(h) Save extension"
    - 999.2-UI-SPEC.md section "Backward-Compatibility & State Contract"
    - solver_core/src/pda_analysis_software/loads/code_packs.py (PSI0_IMPOSED keys — the valid categories)
    - CONTEXT D-02, D-03 and RESEARCH "What to add" item 1 + Pitfall 5
  </read_first>
  <action>
In ui/truss2d/script.js, near `let loads = [];` (line 159), introduce the load-case model:
loadCases (array of {id,name,nature,active,category}), activeCaseId, a makeCaseId() helper, and an
ensureDefaultCase() that, when loadCases is empty, pushes a single {name:'Dead', nature:'Dead',
active:true, category:null} case and sets activeCaseId to it (D-03 default Dead). ensureDefaultCase()
must also repair activeCaseId if it points at a missing case.

The `category` field is only meaningful when a case's nature is 'Imposed' (engine ψ0 lookup,
Plan 01): default 'A-D/H'. For non-imposed natures leave it null/undefined. When a case's nature is
changed TO Imposed and it has no category, default it to 'A-D/H'; when changed away from Imposed,
clear it.

Call ensureDefaultCase() at startup (alongside the existing init / DOMContentLoaded path) and
inside resetAll (script.js:404 — after `loads = [];`, also set loadCases = []; activeCaseId =
null; then call ensureDefaultCase()).

Tag loads at the load-entry push (script.js:257): add `caseId: activeCaseId` to the pushed object
so it becomes `{ nodeId, direction, magnitude, caseId: activeCaseId }`.

Quick-path safety (D-03 / Pitfall 5): solve() (script.js:514) MUST remain byte-functionally
identical — it still posts ALL loads to /solve/truss2d. Do NOT route solve() through the new
combinations endpoint here (that is Wave 5). The case model is additive metadata at this stage.

Save extension — in saveModel() (script.js:1327), add under the existing canvas object (mirror the
memberOverrides additive write at 1381) an optional loadCases array (including each case's
`category`), AND ensure each saved load carries its caseId. Leave the model schema_version
unchanged (additive keys only; the export schema bump is Wave 5).

Backward-compat load — in loadModelFromJson (script.js:1595-1670), after loads are parsed, mirror
the memberOverrides default-on-read at 1640: if data.canvas && data.canvas.loadCases, restore
loadCases (including category) and set activeCaseId to the first active (or first) case; otherwise
call ensureDefaultCase() and assign every legacy load caseId = activeCaseId. Never leave a load
without a caseId and never leave loadCases empty.
  </action>
  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && node --check ui/truss2d/script.js && grep -q "activeCaseId" ui/truss2d/script.js && grep -q "ensureDefaultCase" ui/truss2d/script.js && grep -q "caseId: activeCaseId" ui/truss2d/script.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - node --check ui/truss2d/script.js exits 0
    - script.js contains activeCaseId, ensureDefaultCase, and "caseId: activeCaseId"
    - script.js case objects carry a `category` field (default 'A-D/H' when nature becomes Imposed)
    - script.js loadModelFromJson references loadCases (default-on-read) near the memberOverrides block
    - solve() still posts to /solve/truss2d (grep confirms the route present and unchanged in solve())
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Load-case model exists with default Dead case, imposed category field, per-case tagging, additive save and legacy default-to-Dead load; quick-solve path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Load-Case panel (C1/C2) + imposed category selector + colour-by-nature canvas + Display toggle</name>
  <read_first>
    - ui/truss2d/index.html lines 200-245
    - ui/truss2d/script.js lines 589-700 (draw + load-arrow rendering), 1786-1875 (setupCardFloat)
    - ui/truss2d/style.css (token block; .panel-section, .tool-btn, .support-btn rules)
    - 999.2-UI-SPEC.md sections "C1 — Load-Case panel contract", "C2", "Color" (canvas-nature values), A11y note
    - 999.2-PATTERNS.md sections "(a) Panel section + float button", "(b) Mode visibility auto-enable", "(i) CSS tokens"
    - MEMORY feedback_check_render_toggle_first (tie any new glyph to a Display checkbox)
  </read_first>
  <action>
index.html — insert a new section.panel-section directly ABOVE Material Properties (line 202)
titled "Load Cases" (C1). It contains: an active-case selector (C2) labelled "Active case" as a
select with id activeCaseSelect; a container div id caseTableBody for case rows; and an "Add case"
.tool-btn. Each glyph-only button (delete cross, nature select chevron, float arrow) MUST carry an
aria-label (UI-SPEC A11y note). In the Display toggles section (line 212), add two checkboxes
following the existing onchange="draw()" convention: id chkColourByNature labelled "Colour loads
by case" and id chkActiveCaseOnly labelled "Show active case only".

style.css — add four canvas-nature tokens (Self weight #78909c, Dead #455a64, Imposed #b9770e,
Wind #1f8fb5) in the light :root block AND their dark values (#90a4ae, #b0bec5, #e0a93b, #4db6d6)
in the [data-theme="dark"] block. Add minimal case-row styling reusing existing spacing/border
tokens (flex row matching the .support-btn pattern; a 10px nature swatch). No new spacing tokens.

script.js — add a renderCaseTable() that rebuilds caseTableBody (one row per case: nature swatch,
editable name, nature select chevron, "N loads" count, delete cross) and repopulates
activeCaseSelect; wire the selector change to set activeCaseId then re-render and draw(). Add
handlers: add case (push new case, default nature Dead, make active), rename (inline), set-nature
(from the 4-nature select; render any extra enum values without code change per D-02), delete (if
the case has loads, confirm per UI-SPEC destructive copy: offer reassign-to-Dead or
delete-with-loads; never silently drop loads). When a row's nature is 'Imposed', render an extra
category selector (`<select>` with aria-label "Imposed category") offering "A–D/H (offices/floors)"
(value "A-D/H", default) and "E (storage)" (value "E_storage"); changing it updates that case's
`category`. Non-imposed rows show no category control. Load count per case = number of loads whose
caseId matches. Call renderCaseTable() at startup and after load/reset. In draw() (script.js:589),
when chkColourByNature is checked, colour each load arrow by its case's nature via getComputedStyle
on the canvas-nature token (fallback to the existing canvas-load green when off); when
chkActiveCaseOnly is checked, only draw loads whose caseId === activeCaseId. setupCardFloat
auto-iterates .panel-section so the new panel floats with no extra wiring.
  </action>
  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && node --check ui/truss2d/script.js && grep -q "Load Cases" ui/truss2d/index.html && grep -q "activeCaseSelect" ui/truss2d/index.html && grep -q "chkColourByNature" ui/truss2d/index.html && grep -q "canvas-nature" ui/truss2d/style.css && grep -q "renderCaseTable" ui/truss2d/script.js && grep -q "aria-label" ui/truss2d/index.html && grep -qi "E_storage\|A-D/H" ui/truss2d/script.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - node --check ui/truss2d/script.js exits 0
    - index.html contains "Load Cases", activeCaseSelect, chkColourByNature, chkActiveCaseOnly, and aria-label on glyph-only buttons
    - style.css contains canvas-nature tokens in both :root and [data-theme="dark"]
    - script.js contains renderCaseTable, renders an imposed-only category selector with options "A-D/H" and "E_storage", and reads chkColourByNature/chkActiveCaseOnly in draw()
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Load-Case panel renders cases with active-case selector and an imposed-only category selector; canvas colours loads by nature behind a Display toggle; floats via setupCardFloat; glyph buttons are labelled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — load cases end to end</name>
  <action>Automation for this plan completed in the prior tasks; the executor now pauses for the human to run the browser UAT steps below against the live launchd uvicorn service and confirm each behaves as described. No code is written in this task — it is a verification gate.</action>
  <what-built>
    Typed load-case layer in the truss2d UI: default Dead case, Load-Case panel with active-case
    selector + add/rename/set-nature/delete + imposed category selector, per-case load tagging,
    colour-by-nature canvas (Display toggle), save/load with legacy default-to-Dead, and an
    unchanged quick-solve path.
  </what-built>
  <how-to-verify>
    1. Restart the launchd uvicorn service if needed, then open the truss2d UI (e.g.
       https://catrins-imac.tail568b7e.ts.net/ui/truss2d/index.html or http://127.0.0.1:8000/ui/truss2d/index.html).
    2. Build a small truss, drop a load WITHOUT opening the Load-Case panel, click SOLVE — confirm
       it solves exactly as before (D-03 quick path).
    3. Open "Load Cases": confirm a default "Dead" case exists and is active. Add an "Imposed" and
       a "Wind" case; set the active case to Imposed and drop a load — confirm the case load-count
       increments and the load tags to Imposed.
    4. On the Imposed case row, confirm a category selector appears (A–D/H default, E storage) and
       that the Dead/Wind rows show no category control; switch it to "E (storage)" and confirm it
       sticks.
    5. Tick "Colour loads by case" — confirm loads recolour by nature (Dead blue-grey, Imposed
       ochre, Wind teal). Tick "Show active case only" — confirm only active-case loads draw.
    6. Save the model to JSON, reload the page, Load the JSON — confirm cases + per-case loads +
       the imposed category restore. Then load an OLD pre-phase truss JSON (no loadCases) — confirm
       it loads with all loads assigned to a default Dead case and no errors.
    7. Float the Load-Case panel via the float button — confirm it detaches like other panels.
  </how-to-verify>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
    <manual>Human confirms every how-to-verify step passes in the browser.</manual>
  </verify>
  <done>Human has exercised all how-to-verify steps in the browser and replied "approved".</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied JSON model file → loadModelFromJson | Loaded interchange files (possibly legacy/hand-edited) cross into the UI state here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9992-09 | Tampering | legacy/malformed JSON with no loadCases or dangling caseId | mitigate | default-on-read creates the Dead case and assigns all loads to it; activeCaseId is repaired by ensureDefaultCase (Task 1) — no crash, no orphan loads |
| T-9992-10 | Tampering | unknown nature string in a loaded case | accept | nature is a display/colour key in the UI; unknown values render with the fallback load colour; the server-side Nature guard (Plan 02) rejects bad natures at solve time |
| T-9992-18 | Tampering | unknown imposed category in a loaded/edited case | accept | the UI selector only emits "A-D/H"/"E_storage"; a hand-edited bad category is rejected server-side by the Plan 02 "unknown_category" 422 guard |
</threat_model>

<verification>
- `node --check ui/truss2d/script.js` exits 0
- grep checks in both tasks pass
- Browser UAT (Task 3) approved: quick path unchanged, cases work, imposed category selector works, colour-by-nature works, legacy load defaults to Dead
</verification>

<success_criteria>
- Default Dead case always present; quick-solve path byte-functionally unchanged (D-03)
- Cases can be added/renamed/re-natured/deleted; loads tag to the active case
- Imposed cases expose a category selector (A–D/H / E storage) carried in the model + save/load
- Colour-by-nature + show-active-only Display toggles work
- Additive save/load; legacy files default all loads to Dead
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-load-combination-generator/999.2-03-SUMMARY.md`
</output>
</content>
