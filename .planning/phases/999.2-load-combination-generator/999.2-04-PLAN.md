---
phase: 999.2-load-combination-generator
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - ui/truss2d/script.js
  - ui/truss2d/index.html
  - ui/truss2d/style.css
autonomous: false
requirements: [D-08, D-09, D-14, D-15, D-16, D-17]

must_haves:
  truths:
    - "A shared combination table holds both manual and generated combinations using one data shape {name, cls, terms:[{caseId,factor}]}"
    - "Editing a factor or toggling a combination re-runs superpose+envelope in JS only — no re-solve, no network call, no spinner"
    - "A two-page generator wizard (Parameters then Preview) calls the engine to generate combinations; the UI holds NO gamma/psi values"
    - "Deferred families (SLS frequent/quasi-permanent, EQU, GEO, Accidental/Seismic) and equation sets (6.10a/6.10b) render visible-but-disabled, greyed coming-soon"
    - "Generated preview rows are editable and drop into the same combination table as manual rows"
  artifacts:
    - path: "ui/truss2d/script.js"
      provides: "combinations model, JS superpose+envelope helper, combination table render+edit, wizard logic calling the engine"
      contains: "function superposeJS"
    - path: "ui/truss2d/index.html"
      provides: "Combination table section (C3) + wizard overlay/card (C4) + info-popover scaffold (C5)"
      contains: "wizard-overlay"
    - path: "ui/truss2d/style.css"
      provides: ".wizard-overlay/.wizard-card/.info-btn/.info-popover styles + combination table styles"
      contains: "wizard-card"
  key_links:
    - from: "ui/truss2d/script.js wizard generate"
      to: "POST /solve/truss2d/combinations (generate spec)"
      via: "fetch with generate {code,families}; reads back combinations[].expression"
      pattern: "combinations"
    - from: "ui/truss2d/script.js factor edit handler"
      to: "superposeJS + envelopeJS over cached perCaseForces"
      via: "re-combine cached per-case forces; no network"
      pattern: "perCaseForces"
---

<objective>
Add the manual + generated combination table (C3) and the two-page TSD-style generator wizard
(C4) with reusable (i) info-popovers (C5) to the truss2d UI. The wizard calls the Wave-2 engine
endpoint to generate combinations (D-08 — no gamma/psi in JS); generated rows drop into the same
editable table as manual rows (D-17). Editing factors re-combines cached per-case forces in JS
(superpose+envelope) with no re-solve and no network (D-09 / Pitfall 6).

Purpose: Gives the engineer the combination-building surface — the head-start generator plus full
manual control — feeding the results traceability and export in Wave 5.

Output: modifications to ui/truss2d/{script.js, index.html, style.css}.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/catrinevans/Documents/pda_project/CLAUDE.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-UI-SPEC.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-PATTERNS.md
@/Users/catrinevans/Documents/pda_project/.planning/phases/999.2-load-combination-generator/999.2-RESEARCH.md

<interfaces>
Engine endpoint (Plan 02) the wizard calls — POST /solve/truss2d/combinations request:
```json
{ "nodes":[[x,y]...], "members":[[i,j]...], "E":..., "A":..., "restrainedDoF":[...],
  "cases":[{"id","name","nature","loads":[{"nodeId","direction","magnitude"}]}],
  "combinations":[{"name","cls","terms":[{"caseId","factor"}]}],
  "generate":{"code":"eurocode_uk","families":["STR","SLS"]} }
```
Response:
```json
{ "per_case": {caseId: {"member_forces":[...]}},
  "combinations":[{"name","cls","leading","expression","member_forces":[...]}],
  "envelope": {"members":[{"member","max_tension_N","gov_tension","max_compression_N","gov_compression"}],
               "combo_to_members": {name:[memberIds]}},
  "meta": {...} }
```
Combination data shape shared by manual + generated rows (D-17): `{name, cls, terms:[{caseId, factor}]}`.
Per-case load + forceVector build reused per RESEARCH "(d) Per-case solve + payload" (script.js:538-550).
From Plan 03: loadCases, activeCaseId, the Load-Case panel, setupCardFloat (script.js:1786).
UI-SPEC C3/C4/C5 contracts; greyed deferred families per D-15; copy strings in UI-SPEC Copywriting Contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Combination table (C3) + JS superpose/envelope + manual add/edit/delete</name>
  <read_first>
    - ui/truss2d/index.html lines 200-265 (panel sections + resultsPanel)
    - ui/truss2d/script.js lines 514-580 (solve payload build to reuse per case), 1786-1875 (setupCardFloat)
    - ui/truss2d/style.css (table, .tool-btn, .panel-section, support-tag/chip styles)
    - 999.2-UI-SPEC.md section "C3 — Combination table contract" + Copywriting Contract + "Backward-Compatibility & State Contract" item 3 (no re-solve on edits)
    - 999.2-RESEARCH.md "Pattern 2/3" (superpose/envelope) + Pitfall 6
    - CONTEXT D-01, D-09, D-17
  </read_first>
  <action>
script.js — add a `combinations = []` model (each row {name, cls, terms:[{caseId,factor}], on:true})
and a `perCaseForces = {}` cache (caseId -> member_forces array, populated from the endpoint
response). Implement pure-JS helpers:
- `superposeJS(combo)` -> array length nMembers: sum over terms of `factor * perCaseForces[caseId]`
  (zero vector when a case is missing). This is arithmetic only, NOT factor logic — D-08 is not
  violated (gamma/psi never appear in JS; factors come from the engine or user edits).
- `envelopeJS(activeCombos)` -> per-member {max_tension, gov_tension, max_compression,
  gov_compression} + combo_to_members reverse index (mirror the engine's argmax/argmin).
Add a `recombine()` that recomputes superposeJS for every `on` combination and envelopeJS, then
refreshes the combination table's "governs N" badges and stores the envelope for Wave 5. recombine()
makes NO network call (Pitfall 6) — editing a factor or toggling a row calls recombine() only.

index.html — add a new floatable section.panel-section "Combinations" (C3) below the Load-Case
panel, containing a table with columns: on/off checkbox, name, class chip, terms (factored
expression, font-mono, inline-editable), governs (count badge), delete cross. Header actions:
"Generate combinations..." .tool-btn (opens the wizard, Task 2), "Add row" .tool-btn (manual blank
combination), "Clear generated" .tool-btn (danger). Glyph-only buttons get aria-labels.

script.js — implement `renderCombinationTable()` building the rows; inline-edit a factor updates
the matching term and calls recombine() (no spinner, no fetch); toggling on/off calls recombine();
delete removes the row with the UI-SPEC inline confirm ("Delete combination {name}? This removes it
from the envelope."); "Add row" appends a manual combination using the shared shape; "Clear
generated" removes only generated rows (keep a `generated:true` flag) after the UI-SPEC confirm
("Clear all generated combinations? Manual rows are kept."). Empty-state copy per UI-SPEC.

style.css — add a class chip style (small badge like .support-tag for STR/SLS) and font-mono term
cells; reuse existing table/.tool-btn tokens. No new spacing tokens.
  </action>
  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && node --check ui/truss2d/script.js && grep -q "function superposeJS" ui/truss2d/script.js && grep -q "function recombine" ui/truss2d/script.js && grep -q "perCaseForces" ui/truss2d/script.js && grep -q "Combinations" ui/truss2d/index.html && grep -q "renderCombinationTable" ui/truss2d/script.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - node --check ui/truss2d/script.js exits 0
    - script.js contains superposeJS, envelopeJS, recombine, perCaseForces, renderCombinationTable
    - recombine() contains NO fetch/XHR call (grep the function body: no "fetch(" inside recombine)
    - index.html contains a "Combinations" section with Generate/Add row/Clear generated actions
    - Factor-edit and toggle handlers call recombine() (not the network)
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Shared combination table renders manual+generated rows in one data shape; factor edits/toggles re-combine cached forces in JS with no re-solve; delete/clear honour the destructive copy.</done>
</task>

<task type="auto">
  <name>Task 2: Two-page generator wizard (C4) + (i) info-popover (C5) calling the engine</name>
  <read_first>
    - ui/truss2d/script.js lines 514-580 (build the cases+geometry payload to POST), 1786-1875 (floating-card primitives)
    - ui/truss2d/style.css (.panel-section.floating blur/shadow/border; --shadow-lg, --radius-md, --color-border-subtle, --color-accent-bright tokens)
    - 999.2-UI-SPEC.md sections "C4 — Generator wizard contract", "C5 — info-popover contract", Color (accent reserved list, single-yellow rule), Copywriting Contract
    - 999.2-RESEARCH.md Open Question 2 (generate-only preview, no solve) + EN 1990 clause citations for the (i) notes
    - CONTEXT D-08, D-14, D-15, D-16, D-17
  </read_first>
  <action>
index.html — add a `.wizard-overlay` scrim div (hidden by default) containing a `.wizard-card`
(~420px, centered) with a close cross (aria-label). Two pages inside the card:
- Page 1 (Parameters): Code select (Eurocode (EN 1990) enabled; BS / ASCE / NBR disabled
  "coming soon" — D-11); Country/NA select (UK enabled; others disabled); family checkboxes
  STR-ULS (checked, default on) and SLS-characteristic (checked, default on); SLS frequent, SLS
  quasi-permanent, EQU, GEO, Accidental/Seismic all rendered DISABLED + greyed + labelled
  "coming soon" (D-15 — present from day one, never restructured); equation set 6.10 (checked),
  6.10a/6.10b disabled "coming soon". Each option has a C5 (i) info button. Footer: Next button
  (accent-bright primary).
- Page 2 (Preview): rows grouped by class (STR group, SLS group), each editable (toggle, rename,
  inline factor), showing the font-mono expression + a plain leading-action tag ("Imposed
  leading"). Degenerate Dead+Imposed shows the single 1.35·G + 1.5·Q row with the UI-SPEC info
  note (not an error). Footer: Back (neutral) + "Generate → table" (accent-bright; the phase
  Primary CTA).

style.css — add `.wizard-overlay` (scrim rgba(0,0,0,0.32)), `.wizard-card` (reuse
.panel-section.floating blur/shadow/border family, --radius-md), `.info-btn` (16px circle, accent
glyph, 24px hit area), `.info-popover` (small floating blur card, max-width 260px). Wizard primary
buttons use --color-accent-bright (NOT the SOLVE yellow — that stays the only yellow). Dark-theme
values for any new color.

script.js — implement the wizard controller:
- Open from the C3 "Generate combinations..." button; Esc and outside-click close it; scrim blocks
  canvas clicks (mirror the body.card-dragging pointer-events discipline).
- Next advances to Page 2 by calling the engine GENERATE-ONLY: POST the current geometry + cases +
  `generate:{code:'eurocode_uk', families:[checked]}` to /solve/truss2d/combinations and read back
  `combinations[]` (name, cls, leading, expression, member_forces) AND cache `per_case` into
  perCaseForces. (Per RESEARCH OQ2 the preview uses the engine's returned definitions; the solve
  needed for forces happens in the same call so the table is immediately envelope-ready.) JS holds
  NO gamma/psi — all factors come from the response (D-08).
- "Generate → table" pushes the (possibly edited) preview rows into the shared `combinations` model
  (Task 1) flagged generated:true, closes the wizard, calls renderCombinationTable() + recombine().
- Implement ONE reusable info-popover used by every (i) trigger: content = bold EN 1990 clause
  citation (e.g. "EN 1990 §6.4.3.2, Eq 6.10") + a one-line "when to use"; only one open at a time;
  Esc / outside-click dismiss; aria-expanded on the trigger.
Surface engine 422s (unknown_case / no_loaded_cases) with the UI-SPEC error copy via setStatus.
  </action>
  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && node --check ui/truss2d/script.js && grep -q "wizard-overlay" ui/truss2d/index.html && grep -q "wizard-card" ui/truss2d/style.css && grep -q "info-popover" ui/truss2d/style.css && grep -q "coming soon" ui/truss2d/index.html && grep -q "Generate" ui/truss2d/index.html && grep -q "/solve/truss2d/combinations" ui/truss2d/script.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - node --check ui/truss2d/script.js exits 0
    - index.html contains wizard-overlay, a two-page wizard card, and at least 5 "coming soon" disabled options (deferred families + 6.10a/b)
    - style.css contains wizard-card, info-btn, info-popover; wizard primary uses --color-accent-bright (no new yellow)
    - script.js posts to /solve/truss2d/combinations with a generate spec and reads combinations[].expression; caches per_case into perCaseForces
    - script.js contains no hardcoded 1.35/1.5/0.7/0.6 factor literals in the wizard generate path (factors come from the engine — D-08)
    - The verify command prints PASS
  </acceptance_criteria>
  <done>Two-page wizard generates via the engine (no gamma/psi in JS), shows greyed deferred families, previews editable rows, and drops them into the shared table; reusable (i) popovers cite EN 1990 clauses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser UAT — combination table + generator wizard</name>
  <action>Automation for this plan completed in the prior tasks; the executor now pauses for the human to run the browser UAT steps below against the live launchd uvicorn service and confirm each behaves as described. No code is written in this task — it is a verification gate.</action>
  <what-built>
    Shared combination table (manual + generated), instant JS re-combine on factor edits (no
    re-solve), and a two-page generator wizard with greyed deferred families and (i) info-popovers
    that calls the engine for factors.
  </what-built>
  <how-to-verify>
    1. Restart the launchd uvicorn service (so the Wave-2 endpoint is registered), open the truss2d
       UI, build a truss, and define Dead + Imposed + Wind cases each with a load (Wave-3 panel).
    2. Open "Generate combinations..." — confirm Page 1 shows Eurocode selectable, BS/ASCE/NBR and
       the deferred families/equation sets greyed "coming soon", and STR + SLS checked by default.
       Click an (i) button — confirm a popover with an EN 1990 clause citation appears.
    3. Click Next — confirm Page 2 previews ULS-1 (Imposed leading, +0.9·Wind), ULS-2 (Wind
       leading, +1.05·Imposed), and the SLS rows, with font-mono expressions + leading tags.
    4. Click "Generate → table" — confirm the rows land in the Combinations table identical to
       manual rows. Inline-edit a factor — confirm the "governs" badges update INSTANTLY with no
       spinner and no network request (check the browser Network tab: no fetch on edit).
    5. "Add row" a manual combination, then "Clear generated" — confirm only generated rows clear
       and the manual row remains (per the confirm dialog).
    6. Degenerate check: delete Wind, regenerate — confirm a single 1.35·G + 1.5·Q row with the
       info note rather than an error.
  </how-to-verify>
  <verify>
    <automated>node --check ui/truss2d/script.js</automated>
    <manual>Human confirms every how-to-verify step passes in the browser.</manual>
  </verify>
  <done>Human has exercised all how-to-verify steps in the browser and replied "approved".</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → /solve/truss2d/combinations | The wizard posts geometry + cases + generate spec; the server validates (Plan 02). Client-side factor edits never reach gamma/psi logic (engine owns it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9992-11 | Tampering | user-edited factor cell containing a non-number | mitigate | parse + validate the factor on edit; blank/NaN shows the UI-SPEC inline error "Enter a number for every factor." and is not committed to the term |
| T-9992-12 | Elevation/Logic | JS re-implementing gamma/psi (drift from the engine) | mitigate | wizard generate path holds NO factor literals; all factors come from the engine response (D-08); acceptance criterion greps for absence of 1.35/1.5/0.7/0.6 literals in that path |
| T-9992-13 | Denial of Service | wizard scrim trapping focus / blocking canvas | accept | Esc + outside-click + close button always dismiss; scrim is the one justified modal per UI-SPEC |
</threat_model>

<verification>
- `node --check ui/truss2d/script.js` exits 0
- grep checks in both implementation tasks pass
- Browser UAT (Task 3) approved: wizard generates via engine, deferred families greyed, factor edits re-combine instantly with no network, degenerate case shows single row
</verification>

<success_criteria>
- One shared editable table for manual + generated combinations (D-17)
- Factor edits/toggles re-combine in JS only — no re-solve, no network (D-09)
- Two-page wizard generates via the engine; no gamma/psi in JS (D-08); deferred families greyed (D-15)
- (i) popovers cite EN 1990 clauses (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-load-combination-generator/999.2-04-SUMMARY.md`
</output>
