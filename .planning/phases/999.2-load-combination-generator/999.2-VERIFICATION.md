---
phase: 999.2-load-combination-generator
verified: 2026-06-27T21:05:00Z
status: passed
score: 22/22 locked decisions (D-01..D-22) verified
re_verification: false
---

# Phase 999.2: Load Combination Generator Verification Report

**Phase Goal:** Engineers can define typed load cases (permanent/variable/wind) and generate EN 1990 (UK NA) ULS (STR, 6.10) and SLS-characteristic load combinations; the combination engine produces design forces (and SLS δ_max) via linear superposition, enveloped with per-member governing-combination traceability, exposed via an additive API endpoint and a truss2d UI. Existing quick-solve path and /solve/truss2d stay byte-identical (no-regression).
**Verified:** 2026-06-27T21:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (locked decisions D-01..D-22 as the requirement set)

| #    | Truth (decision)                                                                 | Status     | Evidence |
| ---- | -------------------------------------------------------------------------------- | ---------- | -------- |
| D-01 | Factors live ONLY on combinations, never on loads/cases                          | ✓ VERIFIED | `CombinationTerm.factor`; `LoadCase` has no factor field (combinations.py:40-44, 31-38) |
| D-02 | Code-agnostic physical natures enum (Self weight, Dead, Imposed, Wind)           | ✓ VERIFIED | natures.py `Nature(str, Enum)` 4 members + growth comment; UI selector emits keys |
| D-03 | Default active Dead case; quick-solve never bottlenecked                          | ✓ VERIFIED | `ensureDefaultCase()` (script.js:192-199) at init/reset/load; `solve()` posts all loads unchanged; truss2d route byte-identical |
| D-04 | Self weight nature placeholder; auto-compute deferred                            | ✓ VERIFIED | `Nature.SELF_WEIGHT` present, no auto-computation (by design) |
| D-05 | Engine in solver_core, pure (no matplotlib/print/visualization)                  | ✓ VERIFIED | `loads/` package; grep clean (only a docstring states the rule) |
| D-06 | Additive endpoint; `/solve/truss2d` untouched                                    | ✓ VERIFIED | `POST /solve/truss2d/combinations` (app.py:313); **zero deletions** in app.py vs pre-phase commit b984db5 |
| D-07 | Solver-agnostic, importable by marimo (single source of truth for γ/ψ)           | ✓ VERIFIED | Engine consumes only per-case `member_forces`/`UG`; factor tables only in code_packs.py |
| D-08 | UI calls engine; no factor logic in JS                                            | ✓ VERIFIED | Wizard fetches `/solve/truss2d/combinations`; terms reconstructed from engine `expression`; no γ/ψ literals in generate path (UAT-approved) |
| D-09 | Linear superposition, no re-solve                                                 | ✓ VERIFIED | `superpose`/`superposeJS`; `recombine()` contains no fetch/XHR; test 4 linearity proof passes (rel=1e-9) |
| D-10 | Code-pack abstraction (nature→action, γ, ψ, generation rule)                     | ✓ VERIFIED | `CodePack` frozen dataclass (code_packs.py:30-40) |
| D-11 | Seam built now; Eurocode pack only                                                | ✓ VERIFIED | `_REGISTRY = {"eurocode_uk": EUROCODE_UK}` + `get_code_pack` guard |
| D-12 | 6.10 STR-ULS leading/accompanying iteration; degrades to single 1.35G+1.5Q       | ✓ VERIFIED | Spot-check: Dead+Imposed+Wind → 2 STR combos (Wind=0.9, Imposed=1.05); Dead+Imposed → 1 combo (1.5) |
| D-13 | SLS-characteristic γ=1.0 + δ_max                                                  | ✓ VERIFIED | Spot-check SLS γ=1.0 (SLS-1 Dead=1.0, SLS-2 Imposed=0.7); `superposeDispJS`+`slsEnvelopeDeltaMax`; combo `delta_max_m` in API response; test 3 displacement linearity |
| D-14 | Two-page wizard with (i) info popups                                              | ✓ VERIFIED | `wizard-overlay` (index.html), `info-btn`/`info-popover`, `wizardNext`/`wizardBack` (UAT-approved) |
| D-15 | Deferred families/equation sets greyed "coming soon"                              | ✓ VERIFIED | 11 "coming soon" occurrences in index.html (SLS freq/quasi, EQU, GEO, Accidental/Seismic, 6.10a/b) |
| D-16 | Page 2 editable preview, expression + leading tag                                | ✓ VERIFIED | `renderWizardPreview`/`buildPreviewRow` (UAT-approved); engine returns `expression`+`leading` |
| D-17 | Shared editable combination table, one data shape {name,cls,terms}               | ✓ VERIFIED | `combinations` model rows `{id,name,cls,terms,on,generated,leading}` |
| D-18 | Provenance-carrying envelope (worst force + governing combo)                      | ✓ VERIFIED | `EnvelopeMember.gov_tension`/`gov_compression`; API member0 carries gov_tension=SLS-2, gov_compression=ULS-1; test 5 |
| D-19 | Bidirectional: member→combo forward map AND combo→members reverse index          | ✓ VERIFIED | `combo_to_members` (combinations.py:226); UI member-detail strip + canvas `--canvas-governing` halo |
| D-20 | Results selector [per-case]/[per-combination]/[ENVELOPE] + governing column      | ✓ VERIFIED | `resultsViewSelector` (index.html), `forcesForView`, gov-col (UAT-approved) |
| D-21 | Export: per-member governing string, schema 1.2, top-level retained, non-breaking| ✓ VERIFIED | `exportAnalysis` schema_version '1.2' (script.js:1975), per-member `load_combination` (1883) via `governingComboFor`, top-level retained (1978); `force_kN`/`A_cm2` untouched |
| D-22 | Portable to frame2d/3D (shared structures)                                        | ✓ VERIFIED | Engine touches only per-case arrays; no truss-only assumptions; design-level |

**Score:** 22/22 locked decisions verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `solver_core/.../loads/natures.py` | Nature + ActionType enums | ✓ VERIFIED | 31 lines, str-valued enums, 4 natures |
| `solver_core/.../loads/code_packs.py` | CodePack + EUROCODE_UK + PSI0_IMPOSED + get_code_pack | ✓ VERIFIED | 67 lines; γG=1.35, γG,inf=1.0, γQ=1.5, Wind ψ0=0.6, PSI0_IMPOSED A-D/H=0.7/E_storage=1.0, ξ=0.925 |
| `solver_core/.../loads/combinations.py` | dataclasses + generate_eurocode + superpose + superpose_displacement + delta_max + envelope + generate_for | ✓ VERIFIED | 227 lines; all functions present, all pure |
| `api_server/app.py` | additive endpoint + Pydantic models + 6 guards | ✓ VERIFIED | `/solve/truss2d/combinations` + 6 `cause=` 422 guards; existing route untouched |
| `tests/test_load_combinations.py` | 7 analytical cases | ✓ VERIFIED | 7 `def test_` functions |
| `tests/test_load_combinations_api.py` | TestClient endpoint tests | ✓ VERIFIED | 9 `def test_` functions |
| `ui/truss2d/{script.js,index.html,style.css}` | case table, wizard, results selector, export 1.2 | ✓ VERIFIED | node --check clean; all grep markers present (UAT-approved) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| generate_eurocode | code_packs gamma/psi0/psi0_imposed | reads pack tables | ✓ WIRED | `_psi0_for` resolves category; spot-check correct factors |
| endpoint | engine.solve("truss2d") | per-case solve, caches forces+UG | ✓ WIRED | API spot-check returns per_case d/i/w |
| endpoint | loads.combinations functions | imports + calls | ✓ WIRED | app.py:17-19 imports; response carries combinations+envelope |
| wizard | POST /solve/truss2d/combinations | fetch generate spec | ✓ WIRED | `wizardNext` posts generate; reads expression (UAT) |
| factor edit | superposeJS+envelopeJS (cached) | recombine, no network | ✓ WIRED | `recombine()` has no fetch (D-09) |
| exportAnalysis | per-member governing string | governingComboFor | ✓ WIRED | per-member `load_combination` + schema 1.2 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| /solve/truss2d/combinations | combinations[], envelope | engine.solve N times + superpose/envelope | Yes — real forces (member0 maxT=-1343.5N gov=SLS-2, maxC=-2757.7N gov=ULS-1) | ✓ FLOWING |
| ENVELOPE results view | comboEnvelope.perMember | superposeJS over cached perCaseForces | Yes — recombine refreshes view | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Engine 6.10 STR + SLS + degenerate + Cat-E | python3 engine harness | STR=2, SLS γ=1.0, degenerate=1, CatE=1.5; expression "1.35·Dead + 1.5·Wind + 1.05·Imposed" | ✓ PASS |
| Endpoint full response (TestClient) | POST /solve/truss2d/combinations | 200; 4 combos w/ delta_max_m+expression; envelope w/ gov + combo_to_members | ✓ PASS |
| Quick-path no-regression | POST /solve/truss2d | 200; keys solver/UG/FG/member_forces/meta | ✓ PASS |
| Full test suite | python3 -m pytest -q | 87 passed | ✓ PASS |
| JS syntax | node --check script.js | exit 0 | ✓ PASS |

### No-Regression Invariants (hard rules)

| Invariant | Status | Evidence |
| --------- | ------ | -------- |
| solver_core/loads/ no matplotlib/print/visualization | ✓ PASS | grep clean (only docstring mentions rule) |
| solvers/truss2d.py byte-identical | ✓ PASS | `git diff b984db5..HEAD` empty |
| tests/test_truss2d.py byte-identical | ✓ PASS | `git diff` empty |
| /solve/truss2d + Truss2DRequest byte-identical | ✓ PASS | zero deletion lines in app.py since pre-phase commit |
| Full suite green | ✓ PASS | 87 passed (was 71 pre-phase; +16 new: 7 engine + 9 API) |

### Requirements Coverage

No formal REQUIREMENTS.md REQ-IDs map to this backlog phase. The 22 locked decisions D-01..D-22 serve as the authoritative requirement set and are all SATISFIED (see Observable Truths table). All requirement IDs declared in plan frontmatter (D-01..D-22, fully covered across plans 01-05) are accounted for; no orphaned decisions.

### Anti-Patterns Found

None blocking. Intentional wired-but-quiescent carry-forwards (documented, not stubs):
- `CodePack.psi1/psi2/xi` and `CodePack.generate` slot — carried for deferred SLS frequent/quasi-permanent + 6.10a/b (D-15 deferred).
- `EnvelopeMember.max_compression`/`gov_compression` — wired now, becomes engineering-meaningful after deferred wind-uplift load reversal.
- Known limitation (Plan 04): `reconstructTerms` maps repeated-same-nature cases by case order; distinct-nature setups (the standard scenario) are exact. The clean fix (endpoint returning `terms`) is deferred to a future api change — out of this phase's UI-only scope. Not goal-blocking.

### Human Verification Required

None outstanding. The 3 UI plans (03/04/05) carried `checkpoint:human-verify` items which were **browser-UAT-approved by the user this session** (per verification scope). Automated code-level presence + no-regression invariants all pass.

### Gaps Summary

No gaps. All 22 locked decisions are delivered in the actual codebase, not merely claimed:
- The pure solver_core engine (`loads/`) carries the exact EN 1990 / UK NA factor values, category-aware imposed ψ0, 6.10 STR + SLS-characteristic generation, force AND displacement superposition, and a provenance-carrying envelope with a reverse index — proven by 7 analytical tests and a direct engine spot-check producing the canonical D-21 expression.
- The additive endpoint orchestrates per-case solves through the unchanged adapter and returns the full D-20/D-21 surface — proven by an offline TestClient spot-check returning 4 combinations with per-combination δ_max and a provenance envelope, with the quick path still returning 200.
- The truss2d UI delivers the case table, generator wizard (deferred families greyed), combination table, results selector with governing column + reverse-index canvas highlight, SLS δ_max wiring, and an additive schema-1.2 export with per-member governing strings — code-level verified and user-UAT-approved.
- All no-regression invariants hold: solver, existing test, and `/solve/truss2d` route/request are byte-identical vs the pre-phase commit; full suite 87 passed.

---

_Verified: 2026-06-27T21:05:00Z_
_Verifier: Claude (gsd-verifier)_
