---
phase: 05-revit-tier-1-geometry-exporter
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md
autonomous: false
requirements:
  - REVIT-T1-01
  - REVIT-T1-02
  - REVIT-T1-03
  - REVIT-T1-04
  - REVIT-T1-05

must_haves:
  truths:
    - "A human operator launches Revit 2023/2024/2025 (whichever is installed), opens a drafting view, draws test detail-line fixtures, and clicks the Export to PDA button"
    - "On first click in a Revit session, the '2D TRUSSES AND 2D FRAMES ONLY' TaskDialog appears with a 'Don't show again this session' checkbox (REVIT-T1-02)"
    - "Ticking the checkbox and clicking OK suppresses the warning on all subsequent clicks within the same Revit session (REVIT-T1-02 session persistence)"
    - "Clicking the button with a non-drafting view active (e.g. floor plan) shows 'Active view must be a drafting view (found: ...)' and aborts (REVIT-T1-02 view-type refusal)"
    - "Clicking with an empty drafting view shows the 'No detail lines found' dialog and does NOT write a file (D-04)"
    - "A portal-frame fixture (6 lines: left column, beam, right column, three non-connecting internal lines OR equivalent) exports with correct node count (4 nodes for a clean portal) and correct connectivity (1mm tolerance merges validate REVIT-T1-03)"
    - "A T-junction fixture (one line ending on the mid-span of another) exports as 3 members sharing the interior node (D-05 split)"
    - "A mid-span crossing fixture (two lines crossing without touching endpoints) exports with a crossings-warning in the success TaskDialog and leaves both members unsplit (D-06)"
    - "The exported JSON opens in `ui/frame2d/index.html` via the Load button without error; the canvas populates with the expected geometry (REVIT-T1-05)"
    - "The user can add a fixed support at one node and a point load at another node in the frame2d UI, click Solve, and see a non-error result (REVIT-T1-05 round-trip)"
  artifacts:
    - path: ".planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md"
      provides: "Human-executed UAT record: step-by-step checklist, outcomes, screenshots, exported JSON samples, pass/fail verdict per REVIT-T1 requirement"
      contains_all:
        - "REVIT-T1-01"
        - "REVIT-T1-02"
        - "REVIT-T1-03"
        - "REVIT-T1-04"
        - "REVIT-T1-05"
        - "status:"
  key_links:
    - from: "Revit (user session)"
      to: "ExportToPDA.pushbutton"
      via: "PDA_Tools.tab → Analytical.panel → col1.stack → Export to PDA button click"
      pattern: "Export to PDA"
    - from: "Exported JSON file"
      to: "ui/frame2d/index.html Load button"
      via: "browser file-input → data.canvas.nodes/members → canvas populates"
      pattern: "Load"
    - from: "frame2d UI Solve button"
      to: "/solve/frame2d API endpoint"
      via: "POST payload from UI state → FrameV2Adapter → BeamBarStructure_v2 → AnalysisResult"
      pattern: "/solve/frame2d"
---

<objective>
Run the Phase 5 human UAT. The operator launches Revit, drives the `Export to PDA` button across six canonical fixtures (empty view, non-drafting view, single line, portal frame, T-junction, mid-span crossing), records outcomes in `05-HUMAN-UAT.md`, loads the exported JSON files in `ui/frame2d/index.html`, adds supports and a load, clicks Solve, and verifies a non-error result. This plan delivers final live-in-Revit verification for REVIT-T1-01, REVIT-T1-02, REVIT-T1-03, REVIT-T1-04, and REVIT-T1-05. REVIT-T1-04 is re-verified explicitly in Fixture 3 via a human inspection step that opens the exported JSON in a text editor and confirms (a) every `canvas.nodes[*].x` / `.y` AND top-level `nodes[*]` coordinate has at most 4 decimal places, and (b) no `z` field appears anywhere in the file.

Purpose: All prior plans (05-01, 05-02, 05-03) verified the script statically (grep + AST + offline `_build_json` dry-run). This plan is the only opportunity to run the button in a real Revit session. Without it, the button could still fail at runtime due to:
- pyRevit re-execution behaviour not matching the session-flag assumption (RESEARCH Assumption A1)
- `lib/Snippets/` not actually being on `sys.path` (Open Question 4)
- `BuiltInCategory.OST_Lines` not collecting detail lines reliably (Assumption A3)
- Revit 2025 behavioural surprise not captured in the research (Assumption A4)

Output: `05-HUMAN-UAT.md` containing pass/fail verdicts per requirement, any bug reports, and optionally a sample exported JSON committed to `.planning/phases/05-revit-tier-1-geometry-exporter/fixtures/` for future regression reference.
</objective>

<execution_context>
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/workflows/execute-plan.md
@/Users/catrinevans/Documents/pda_project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-revit-tier-1-geometry-exporter/05-CONTEXT.md
@.planning/phases/05-revit-tier-1-geometry-exporter/05-01-SUMMARY.md
@.planning/phases/05-revit-tier-1-geometry-exporter/05-02-SUMMARY.md
@.planning/phases/05-revit-tier-1-geometry-exporter/05-03-SUMMARY.md
@.planning/phases/04-2d-frame-solver-ui-hardening/04-HUMAN-UAT.md

<cross_repo_note>
This plan's ONE output file (`05-HUMAN-UAT.md`) lives in pda_project — commit it here.
No Revit-side code is edited. If the human UAT surfaces a bug, the fix lives in the sibling repo `CustomRevitExtension/` and reopens plan 05-03 (or prompts a dedicated gap-closure plan).
</cross_repo_note>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Human operator runs six live UAT fixtures in Revit and records outcomes</name>
  <files>.planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md</files>
  <read_first>
    - /Users/catrinevans/Documents/pda_project/.planning/phases/05-revit-tier-1-geometry-exporter/05-CONTEXT.md (D-01..D-16 — ALL user decisions being validated)
    - /Users/catrinevans/Documents/pda_project/.planning/phases/05-revit-tier-1-geometry-exporter/05-RESEARCH.md (§Common Pitfalls, §Assumptions Log, §Open Questions)
    - /Users/catrinevans/Documents/pda_project/.planning/phases/05-revit-tier-1-geometry-exporter/05-03-SUMMARY.md (path to any offline-generated fixture JSON)
    - /Users/catrinevans/Documents/pda_project/.planning/phases/04-2d-frame-solver-ui-hardening/04-HUMAN-UAT.md (style reference for the UAT record format)
  </read_first>

  <action>
    Human-driven UAT. Executor writes the UAT scaffold file, prints the six-fixture checklist, pauses execution via the human-action checkpoint, waits for the human to drive Revit end-to-end and fill in the UAT record, then resumes and marks the task done after verifying the file status is not pending. See <how-to-verify> for the full six-fixture protocol and scaffold template, and <resume-signal> for accepted completion replies.
  </action>

  <what-built>
    After plans 05-01..05-03, the sibling repo has a working pyRevit pushbutton at
    `/Users/catrinevans/Documents/CustomRevitExtension/PDA_customRevit.extension/PDA_Tools.tab/Analytical.panel/col1.stack/ExportToPDA.pushbutton/`
    with `script.py`, `icon.png`, `bundle.yaml`. All static checks pass. All
    offline algorithm checks pass. The `_build_json` function has been validated
    against a 3-node L-shape payload. Remaining unknowns are all live-in-Revit
    behaviours.
  </what-built>

  <how-to-verify>
    The executor agent SHOULD:
    1. Prepare: verify `uvicorn api_server.app:app --reload` starts the API server on http://127.0.0.1:8000/health and reports `{"status":"ok","solvers":[...]}` including `frame2d` and `truss2d`.
    2. Write the UAT scaffold file (see Step 7 below for the template) before pausing.
    3. Pause execution and print the full 6-fixture checklist below so the human can drive Revit.

    ### The human operator runs these six fixtures:

    **Pre-flight (once per session):**
    - Open Revit 2023, 2024, or 2025 (whichever is installed on the user's machine).
    - Ensure pyRevit has reloaded since the sibling-repo commit in plan 05-01 landed (`Reload` button in the pyRevit tab, or restart Revit).
    - Confirm the button "Export to PDA" appears in `PDA_Tools` tab → `Analytical` panel → `col1.stack` alongside Loads, Supports, StructuralAnalyticalModel.

    **Fixture 1 — Non-drafting view guard (REVIT-T1-02 view-type refusal):**
    1. Open a floor plan (e.g. Level 1) — a non-drafting view.
    2. Click the Export to PDA button.
    3. EXPECT: TaskDialog appears with title "PDA Export" and message `Active view must be a drafting view (found: ViewPlan).` (or whatever class name Revit reports).
    4. Click OK. EXPECT: no file written, no other dialog, no crash.
    5. Record in 05-HUMAN-UAT.md: view-type guard PASS/FAIL + the exact message seen.

    **Fixture 2 — First-click 2D-only warning (REVIT-T1-02 warning banner + session persistence):**
    1. Create a new drafting view (View tab → Drafting View → give it a test name like `PDA_UAT_1`).
    2. Click the Export to PDA button.
    3. EXPECT: TaskDialog appears with title "PDA Export", main instruction "2D TRUSSES AND 2D FRAMES ONLY", a content area explaining supports/loads are added in the frame2d UI, a `Don't show this message again this session` checkbox, OK and Cancel buttons.
    4. Tick the checkbox, click OK.
    5. EXPECT: the next dialog is "No detail lines found in active drafting view — draw some first." (D-04 empty-view message — the drafting view has no lines yet).
    6. Click OK. Click the Export button AGAIN immediately (no Revit restart).
    7. EXPECT: the 2D-only warning does NOT reappear (session flag works). Only the "No detail lines found" dialog appears.
    8. Record: 2D-only warning banner text PASS/FAIL; session-flag persistence PASS/FAIL; D-04 empty-view dialog PASS/FAIL.

    **Fixture 3 — Single-line export (REVIT-T1-01 minimal happy path):**
    1. In the same drafting view, draw ONE straight detail line roughly from (0, 0) to (5 m, 0 m). Use any line style — D-02 says all styles accepted.
    2. Click Export to PDA.
    3. EXPECT: save-file dialog opens pre-populated with filename `PDA_UAT_1_pda.json` (or similar — the sanitised view name + `_pda`). Pick a Desktop location, click Save.
    4. EXPECT: success TaskDialog "PDA Export Complete" showing `Exported 2 nodes, 1 members to: <full path>` with NO crossing warning.
    5. Open the saved JSON in a text editor. Manually verify:
       - `"schema_version": "1.0"` — present.
       - `"solver": "frame2d"` — EXACT (not `"frame_v2"`).
       - `"nodes"` — 2 entries, each a 2-float array with values rounded to 4 decimals.
       - `"members": [[1, 2]]` — 1-based.
       - `"canvas"."origin": {"x": 100, "y": 400}` — non-null.
       - `"canvas"."nodes"` — 2 objects each with `id`, `x`, `y`, `realX`, `realY`.
       - `"canvas"."members": [{"id": 0, "start": 0, "end": 1, "type": "beam", ...}]` — `start`/`end` are 0-based.
       - `"canvas"."supports": {}`, `"canvas"."nodeLoads": []`, `"canvas"."udl": []`, `"canvas"."memberOverrides": {}`.
       - `"E": 200000000000.0`, `"I": 0.0001`, `"A": 0.01` — default values.
       - **REVIT-T1-04 explicit check:** open the exported JSON in a text editor
         and inspect EVERY `canvas.nodes[*].x` and `canvas.nodes[*].y` field AND
         EVERY entry in the top-level `nodes` array. Every numeric value MUST
         have at most 4 decimal places (e.g. `3.048`, not `3.04800000000002`).
         Then search the entire file for any `z` field — there MUST be none
         (neither `"z":` nor `"Z":` nor any Z coordinate embedded in the
         node arrays). Record the result on the REVIT-T1-04 verdict row.
    6. Record: JSON shape PASS/FAIL with the exported-file path; metres-4dp + no-Z inspection PASS/FAIL for REVIT-T1-04.

    **Fixture 4 — Portal frame + coincident endpoint merge (REVIT-T1-03):**
    1. In a NEW drafting view `PDA_UAT_2`, draw three detail lines:
       - Line A: (0, 0) to (0, 4 m)  — left column
       - Line B: (0, 4 m) to (5 m, 4 m) — beam
       - Line C: (5 m, 4 m) to (5 m, 0) — right column
       The endpoints should be drawn with Revit's snapping enabled so the (0, 4) endpoint of A and B coincide; same for (5, 4) of B and C.
    2. Click Export.
    3. EXPECT success dialog: `Exported 4 nodes, 3 members`. Not 6 nodes — the coincident endpoints MUST merge (REVIT-T1-03 proof).
    4. Open the JSON. Verify `nodes` has exactly 4 entries, sorted lexicographically by (x, y): roughly `[[0, 0], [0, 4], [5, 0], [5, 4]]` (x ascending, then y ascending).
    5. Verify `members` has 3 entries, indices into the 4-node array.
    6. Record: node-merge PASS/FAIL, lexicographic ordering PASS/FAIL (first node at minimum (x,y)).

    **Fixture 5 — T-junction split (D-05):**
    1. In a NEW drafting view `PDA_UAT_3`, draw two detail lines:
       - Line A: (0, 0) to (10 m, 0)  — long horizontal line
       - Line B: (5 m, 0) to (5 m, 3 m) — vertical line ending ON THE MID-SPAN of line A (snap to the endpoint of A if tools permit; the important geometry is that B's (5, 0) endpoint lies on A's interior).
    2. Click Export.
    3. EXPECT success dialog: `Exported 4 nodes, 3 members`. The 3 members are (i) A split left: (0, 0)→(5, 0); (ii) A split right: (5, 0)→(10, 0); (iii) B: (5, 0)→(5, 3).
    4. Open the JSON. Verify 4 nodes, 3 members, and that node (5, 0) appears as an endpoint for three distinct members in `canvas.members`.
    5. Record: T-split PASS/FAIL with screenshot or copy of the relevant JSON excerpt.

    **Fixture 6 — Mid-span crossing warning (D-06):**
    1. In a NEW drafting view `PDA_UAT_4`, draw two detail lines that cross but neither has an endpoint near the crossing:
       - Line A: (0, 0) to (10 m, 10 m)
       - Line B: (10 m, 0) to (0, 10 m)
       The intersection at (5, 5) is interior to both.
    2. Click Export.
    3. EXPECT success dialog: `Exported 4 nodes, 2 members` (NOT 5 nodes, NOT 4 members — the crossing is NOT auto-split per D-06) AND the warning suffix "Warning: 1 mid-span crossing(s) detected and NOT split".
    4. Open the JSON. Verify 4 nodes, 2 members, none of which share an endpoint near (5, 5).
    5. Record: crossing detection PASS/FAIL.

    **REVIT-T1-05 round-trip (critical):**
    6. Start the API server in a terminal: `cd /Users/catrinevans/Documents/pda_project && uvicorn api_server.app:app --reload`.
    7. Open `/Users/catrinevans/Documents/pda_project/ui/frame2d/index.html` in Safari (user's browser per project history) or Chrome.
    8. Click the Load button. Select the JSON from Fixture 4 (portal frame).
    9. EXPECT: canvas populates with the 4-node, 3-member portal frame at the correct scale (1 m = 20 px grid). No JS console errors. Status banner shows a loaded-ok message.
    10. Click the appropriate node/tool buttons in the frame2d UI to add:
        - a `fixed` support at node 1 (bottom-left column base)
        - a `fixed` support at node 3 (bottom-right column base)
        - a point load `Fy = -10000 N` (vertical down) at the top-left or top-right node (beam/column junction)
    11. Click Solve.
    12. EXPECT: a non-error result — green success banner, displacements visible, BMD/SFD rendered. No HTTP 500, no 422.
    13. Record: round-trip PASS/FAIL with the solver's reported max displacement (any reasonable value within an order of magnitude of hand calculation).

    ### Step 7 — the executor writes this scaffold file BEFORE pausing:

    Write `.planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md` with the template:

    ```markdown
    ---
    phase: 05-revit-tier-1-geometry-exporter
    type: human-uat
    started: <ISO date>
    completed: <ISO date after human finishes>
    operator: paulo@pda-structure.co.uk
    revit_version: <2023 | 2024 | 2025 — fill in>
    pyrevit_version: <fill in from Revit → pyRevit tab → About>
    status: pending   # → passed | passed-with-bugs | failed
    ---

    # Phase 5 Human UAT — Revit Tier 1 Geometry Exporter

    ## Summary

    <Filled in by human after all fixtures run.>

    ## Fixture Results

    ### F1 — Non-drafting view guard (REVIT-T1-02 view-type refusal)
    - Outcome: <PASS | FAIL | SKIP>
    - Verdict: <PASS | FAIL>
    - Observed dialog title: <...>
    - Observed dialog message: <...>
    - Notes: <...>

    ### F2 — First-click 2D-only warning + session persistence (REVIT-T1-02)
    - Outcome: <PASS | FAIL | SKIP>
    - Verdict: <PASS | FAIL>
    - Warning banner text: <PASS | FAIL>
    - Session-flag persistence across second click: <PASS | FAIL>
    - D-04 empty-view dialog: <PASS | FAIL>
    - Notes: <...>

    ### F3 — Single-line export (REVIT-T1-01, REVIT-T1-04)
    - Outcome: <PASS | FAIL | SKIP>
    - Verdict: <PASS | FAIL>
    - Success dialog counts: 2 nodes, 1 members — <observed values>
    - JSON shape manual inspection: <PASS | FAIL>
    - REVIT-T1-04 4-dp metres + no-Z inspection: <PASS | FAIL>
    - Exported file: <absolute path — optionally copy to .planning/phases/05-.../fixtures/>
    - Notes: <...>

    ### F4 — Portal frame + endpoint merge (REVIT-T1-03)
    - Outcome: <PASS | FAIL | SKIP>
    - Verdict: <PASS | FAIL>
    - Success dialog counts: expected 4 nodes / 3 members — <observed>
    - Lexicographic node order (D-08): <PASS | FAIL> — first node coords: <...>
    - Exported file: <path>
    - Notes: <...>

    ### F5 — T-junction split (D-05)
    - Outcome: <PASS | FAIL | SKIP>
    - Verdict: <PASS | FAIL>
    - Success dialog counts: expected 4 nodes / 3 members — <observed>
    - T-node shared by 3 members in canvas.members: <PASS | FAIL>
    - Exported file: <path>
    - Notes: <...>

    ### F6 — Mid-span crossing warning (D-06)
    - Outcome: <PASS | FAIL | SKIP>
    - Verdict: <PASS | FAIL>
    - Success dialog counts: expected 4 nodes / 2 members — <observed>
    - Crossing warning text present: <PASS | FAIL>
    - Exported file: <path>
    - Notes: <...>

    ## REVIT-T1-05 Round-trip

    - Fixture used: F4 (portal frame) — <path>
    - Loaded into ui/frame2d/index.html without error: <PASS | FAIL>
    - Canvas populated correctly: <PASS | FAIL>
    - Added supports / load in UI and clicked Solve: <PASS | FAIL>
    - Solve returned non-error result: <PASS | FAIL>
    - Max displacement observed: <float, metres>
    - API server health: <http://127.0.0.1:8000/health output>

    ## Requirement Verdicts

    | Requirement | Verdict | Evidence |
    |-------------|---------|----------|
    | REVIT-T1-01 | <PASS | FAIL> | F3, F4 |
    | REVIT-T1-02 | <PASS | FAIL> | F1, F2 |
    | REVIT-T1-03 | <PASS | FAIL> | F4 |
    | REVIT-T1-04 | <PASS | FAIL> | Fixture 3 JSON inspection (human reads exported file for 4-dp metres, no Z field) |
    | REVIT-T1-05 | <PASS | FAIL> | Round-trip section |
    (REVIT-T1-04 was offline-verified at 4-dp rounding in plan 05-02 and at shape-level in plan 05-03's JSON contract test. F3's explicit inspection step re-confirms both invariants on a live-exported file.)

    ## Bugs / Deviations

    <List any bugs. If present, status MUST be "passed-with-bugs" or "failed" and a gap-closure plan is required before phase completion.>

    ## Screenshots / Attachments

    <Optional. Paste absolute paths to screenshots of Revit dialogs and the frame2d UI post-Solve.>
    ```

    4. Pause and return control to the human with a clear "Please run the 6 fixtures above, fill in 05-HUMAN-UAT.md, and set `status:` to `passed`, `passed-with-bugs`, or `failed` before resuming."
  </how-to-verify>

  <resume-signal>
    Human replies with one of:
    - "UAT passed — set status to passed" (all 6 fixtures PASS, round-trip PASS, no bugs)
    - "UAT passed with bugs — details in 05-HUMAN-UAT.md" (functional but defects noted; phase closes with gap-closure plan handed off)
    - "UAT failed — details in 05-HUMAN-UAT.md" (REVIT-T1-01, REVIT-T1-02, REVIT-T1-03, or REVIT-T1-05 blocking failure; phase blocks, triage required, loop back to plans 05-01/02/03 for fixes)
  </resume-signal>

  <acceptance_criteria>
    - File exists: `test -f .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md`.
    - Frontmatter `status` field is one of `passed`, `passed-with-bugs`, `failed` (NOT `pending`): `grep -E "^status:.*(passed|passed-with-bugs|failed)" 05-HUMAN-UAT.md` matches.
    - All SIX fixture section headers are present: `[ "$(grep -c '^### F[1-6] ' 05-HUMAN-UAT.md)" -ge 6 ]` (scaffold template uses `### F{n} — ...`).
    - All SIX fixtures have a uniform Outcome line (Warning 2 fix — no silent skipping): `[ "$(grep -c '^- Outcome:' 05-HUMAN-UAT.md)" -ge 6 ]`.
    - All SIX fixtures have a uniform Verdict line filled in: `[ "$(grep -c '^- Verdict:' 05-HUMAN-UAT.md)" -ge 6 ]`.
    - No unfilled placeholder outcomes remain in the Outcome/Verdict slots: `! grep -q '^- Outcome: <PASS | FAIL | SKIP>' 05-HUMAN-UAT.md` AND `! grep -q '^- Verdict: <PASS | FAIL>$' 05-HUMAN-UAT.md`.
    - Round-trip section has a Max displacement value (not `<...>`) and a non-error Solve verdict.
    - All FIVE requirement verdict rows filled in (REVIT-T1-01, -02, -03, -04, -05): `! grep -q '<PASS | FAIL>' 05-HUMAN-UAT.md` AND `[ "$(grep -c '^| REVIT-T1-0[1-5] ' 05-HUMAN-UAT.md)" -ge 5 ]`.
    - REVIT-T1-04 explicit check recorded in Fixture 3: `grep -q 'REVIT-T1-04 4-dp metres + no-Z inspection:' 05-HUMAN-UAT.md` matches with a PASS/FAIL value (not the template placeholder).
    - If status is `failed` → task outcome is FAIL and phase cannot close until gap-closure plan lands.
    - If status is `passed-with-bugs` → bugs list is non-empty and each bug has a recommended follow-up (fix in plan 05-03 re-run OR defer to a backlog item).
    - If status is `passed` → all six fixtures show PASS or benign SKIP; round-trip PASS; no open bugs; REVIT-T1-01..05 all PASS in verdict table.
  </acceptance_criteria>

  <verify>
    <automated>cd /Users/catrinevans/Documents/pda_project && test -f .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && grep -qE "^status: (passed|passed-with-bugs|failed)" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && ! grep -q "<PASS | FAIL>" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && [ "$(grep -c '^### F[1-6] ' .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md)" -ge 6 ] && [ "$(grep -c '^- Outcome:' .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md)" -ge 6 ] && [ "$(grep -c '^- Verdict:' .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md)" -ge 6 ] && ! grep -q '^- Outcome: <PASS | FAIL | SKIP>' .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && grep -q "REVIT-T1-01" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && grep -q "REVIT-T1-02" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && grep -q "REVIT-T1-03" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && grep -q "REVIT-T1-04" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md && grep -q "REVIT-T1-05" .planning/phases/05-revit-tier-1-geometry-exporter/05-HUMAN-UAT.md</automated>
  </verify>

  <done>
    `05-HUMAN-UAT.md` exists with status set to passed / passed-with-bugs / failed, all 6 fixture outcomes recorded, round-trip verdict and max-displacement value captured, and all 4 requirement verdict rows filled.
    If passed or passed-with-bugs: Phase 5 is ready for the normal post-phase flow (ROADMAP check-off, phase completion commit).
    If failed: gap-closure plan must be created (rerun `/gsd-plan-phase 5 --gaps`) before phase close.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Revit session → human operator | Human-driven UAT; no automation; no new attack surface. |
| Exported JSON file → frame2d UI | Local filesystem read only; no network transit. |
| frame2d UI → API server | Same-origin localhost POST; no CSRF/auth concerns in the MVP UAT environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | T (Tampering) | Human operator writing UAT record | accept | Single-trusted-user scenario; the UAT file is evidence, not a security artifact. Plan 05-05 (if it existed) or the phase verifier will independently re-run the static grep checks from plans 05-01..05-03. |
| T-05-18 | I (Information Disclosure) | Exported JSON fixtures committed to `.planning/fixtures/` | accept | All fixtures are synthetic test geometry (portal frames, T-junctions) — no sensitive model data. |
| T-05-19 | S/R/E/D | — | N/A | Local UAT session; no threats of note. |

Phase 5 overall: threat model is minimal (desktop tool + local file I/O), comprehensively covered by plans 05-01 (T-05-01..T-05-06), 05-02 (T-05-07..T-05-10), and 05-03 (T-05-11..T-05-16, including explicit path-traversal and unicode-write mitigations).
</threat_model>

<verification>
- UAT file exists with the required frontmatter shape.
- Status is NOT `pending` (human has completed the run).
- All four REVIT-T1 requirement verdicts filled in.
- No unreplaced `<PASS | FAIL>` template placeholders.

**Backstop:** if human UAT fails on any of REVIT-T1-01/02/03/05, phase cannot close until a gap-closure plan lands. The orchestrator enforces this via ROADMAP phase-completion gates.
</verification>

<success_criteria>
- [x] REVIT-T1-01: single-line export (F3) and multi-line portal (F4) both succeed end-to-end — confirmed live.
- [x] REVIT-T1-02: view-type refusal (F1) and 2D-only warning + session persistence (F2) both work as specified.
- [x] REVIT-T1-03: 1mm-merge confirmed by F4 (4 nodes, 3 members) and F5 (T-split adds exactly one interior node).
- [x] REVIT-T1-04: 4-dp rounding and Z-drop already offline-verified in plan 05-03; F3/F4 inspection re-confirms on live-exported files.
- [x] REVIT-T1-05: exported JSON loads in frame2d UI, user adds supports/load, Solve returns non-error result.
- [x] D-04 (empty-view), D-05 (T-split), D-06 (crossing warn), D-08 (lex sort), D-09 (defaults), D-10 (uniform), D-11 (Z dropped), D-12 (metres), D-13 (save dialog), D-14 (success wording), D-15 (fail-fast), D-16 (session-scoped warning) all exercised by the fixture suite.
</success_criteria>

<output>
After human UAT completes, create `.planning/phases/05-revit-tier-1-geometry-exporter/05-04-SUMMARY.md` summarising:
- UAT status (passed / passed-with-bugs / failed)
- Revit version tested (2023, 2024, or 2025)
- Count of fixtures passed vs failed vs skipped
- Max displacement observed in the round-trip Solve step
- Any bugs with proposed disposition (fix-in-place via 05-03 edit | defer to v1.3 backlog item | defer to Phase 6 scope)
- Path to any fixture JSON files committed for regression reference (optional)
- Git SHA of the pda_project planning-artifact commit (05-HUMAN-UAT.md + 05-04-SUMMARY.md)
- Phase completion verdict: green-light for ROADMAP phase check-off, or red-light with gap-closure plan hand-off.
</output>
